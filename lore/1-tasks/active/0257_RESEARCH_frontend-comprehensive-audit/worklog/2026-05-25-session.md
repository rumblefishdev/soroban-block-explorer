# Worklog — 2026-05-25 — Wave 1 (sequential single-agent run)

Operator: karolkow (via single Claude agent)
Wave: 1 (Tier 1 deterministic foundations)
Mode: read-only senior fresh-eye audit
Duration estimate: ~2.5h equivalent (parallel-agent budget 3h, single-agent compressed via batched grep + tooling)

## Sub-phases executed

| # | Sub-phase | Output file | Status |
|---|---|---|---|
| 1.0 | Archaeology — FE lore sweep | `findings/00-archaeology.md` | ✓ done |
| 1.1 | OpenAPI strict adherence | `findings/AF-openapi-adherence.md` | ✓ done |
| 1.11 | P Code quality | `findings/P-code-quality.md` | ✓ done |
| 1.11b | AQ Type safety depth | `findings/AQ-type-safety.md` | ✓ done |
| 1.21 | CO Dependency hygiene | `findings/CO-dependency-hygiene.md` | ✓ done |
| 1.16 | AI Bundle analysis | `findings/AI-bundle-analysis.md` | ✓ done |

## Findings counts per file

| File | Findings | 🔴 | 🟠 | 🟡 | 🟢 |
|---|---:|---:|---:|---:|---:|
| `00-archaeology.md` | 5 critical flags + 28 future-work cross-refs + 7 issue patterns + 41 emerged decisions inventoried | 1 | 2 | 2 | 0 |
| `AF-openapi-adherence.md` | 4 | 0 | 0 | 1 | 3 |
| `P-code-quality.md` | 8 | 0 | 0 | 2 | 3 (+ 3 ✓) |
| `AQ-type-safety.md` | 6 | 0 | 2 | 2 | 1 (+ 1 ✓) |
| `CO-dependency-hygiene.md` | 8 | 0 | 3 | 2 | 3 |
| `AI-bundle-analysis.md` | 9 | 0 | 2 | 3 | 2 (+ 2 ✓) |
| **Total** | **~40 actionable** | **1** | **9** | **12** | **12** |

## Blockers hit

- None. Every command exited 0. `nx run-many -t typecheck` and `nx run-many -t lint` both green. `npm audit`, `npm outdated`, `license-checker`, `nx build` all completed without intervention.
- One operational nit: `timeout` not on macOS path; ran commands without it (every command finished sub-minute).

## Decisions made

1. **Did not run `madge --circular` / `nx graph` cycle check.** Heavy, undocumented for this codebase, and Nx project references already prevent cross-project cycles. Flagged as 🟢 LOW F-P-6 for follow-up if cycles ever surface.
2. **Did not install `knip` / `ts-prune`.** Audit is read-only; flagged absence as 🟡 MEDIUM F-P-2.
3. **Did not run `vite-bundle-visualizer`.** Not in deps; flagged 🟡 MEDIUM F-AI-7. Bundle sizes captured from Vite's own per-chunk table output.
4. **Closed F-AI-5 (React Query devtools prod leak) inline** after one quick grep confirmed `import.meta.env.DEV` guard. Saved a follow-up task spawn.
5. **Skipped CSV outputs (D-state-coverage-matrix.csv, R-responsive-matrix.csv).** Those are Wave 4 / Wave 6 deliverables; Wave 1 scope.

## Top 5 highest-severity findings

1. 🔴 **CRITICAL — A1 (archaeology):** `web/src/pages/TransactionDetailPage.tsx` is a 14-line `PageStub`. Every tx-hash link from every list page lands on a "stub" placeholder. 0070 + 0071 (the real work) sit in backlog with no launch-blocker tag.
2. 🟠 **HIGH — A3 (archaeology):** 25 of 28 documented Future Work items from FE archive tasks have no spawned backlog task. 82% deferral gap.
3. 🟠 **HIGH — F-AQ-1:** `noUncheckedIndexedAccess` not enabled. Single most-valuable type-safety flag missing; directly motivates the F-P-1 non-null-assertion lint warning.
4. 🟠 **HIGH — F-AI-1 + F-AI-2:** Main bundle 594 KB (188 KB gz) exceeds Vite's 500 KB warning, and `LiquidityPoolDetailPage` 313 KB (96 KB gz) — half the eager bundle — for one route. `@mui/x-charts` likely the bulk.
5. 🟠 **HIGH — F-CO-1 + F-CO-6:** Vite 7.3.1 has 3 dev-server CVEs (path traversal / fs-deny bypass / arbitrary file read via WebSocket); patched in 7.3.3 already `wanted`. Separately, `@mui/utils` triple-versioned in lock (`7.3.9`, `9.0.0`, `9.0.1`) because `@mui/material@7` is held back while `@mui/x-charts@9` + `@mui/icons-material@9` moved forward.

## Suggested next wave

Per task README "Execution Strategy → Waves":

- **Wave 2 (Tier 1 cleanup + Tier 2 start):**
  - Agent A: 1.4 API consistency (grep over op enums, NUMERIC, cursor, polling cache headers) + 1.8 Data formatting consistency (grep `formatAmount` / `formatCompact` usage)
  - Agent B: 1.19 Build & deploy hygiene (`.env.example` vs `VITE_*`, `localhost:9000` grep, console-leak in built bundle) + 1.20 Quick wins (status footer, version SHA, footer links)
  - Agent C: 1.18 Q+AR Lore process + commit conventions (frontmatter ↔ body sync, commitlint, branch naming)

  Wave 2 is grep-heavy + can run in parallel like Wave 1.

- **Bonus immediate action items from Wave 1 worth doing before Wave 2 starts:**
  1. Bump Vite 7.3.1 → 7.3.3 (1 line, closes 3 dev-server CVEs). Trivial.
  2. Fix `assetColor.ts:131` non-null assertion lint warning (1 line). Greenest lint.
  3. Verify 0066 task body says "Implemented" — pure lore hygiene; archaeology found this drift.

  All 3 are sub-15-min fixes that don't pollute audit scope.

---

# Wave 2 — Tier 1 cleanup + Tier 2 start (sequential single-agent)

Operator: karolkow (single Claude agent, sequential)
Wave: 2 (Tier 1 cleanup + Tier 2 start)
Mode: read-only senior fresh-eye audit
Duration estimate: ~3h equivalent compressed via batched grep + Read

## Sub-phases executed

| # | Sub-phase | Output file | Status |
|---|---|---|---|
| 1.4 | C API consistency cross-checks | `findings/C-api-consistency.md` | ✓ done |
| 1.8 | J Data formatting consistency | `findings/J-data-formatting.md` | ✓ done |
| 1.18 | Q+AR Lore process + commit conventions | `findings/Q-AR-lore-process-conventions.md` | ✓ done |
| 1.19 | AO Build & deploy hygiene | `findings/AO-build-deploy.md` | ✓ done |
| 1.20 | Quick wins DM+DN+CA | `findings/quick-wins-DM-DN-CA.md` | ✓ done |

## Findings counts per file (Wave 2)

| File | Findings | 🔴 | 🟠 | 🟡 | 🟢 |
|---|---:|---:|---:|---:|---:|
| `C-api-consistency.md` | 20 | 0 | 1 | 1 | 3 (rest ✓) |
| `J-data-formatting.md` | 15 | 0 | 0 | 5 | 2 (rest ✓) |
| `Q-AR-lore-process-conventions.md` | 15 | 0 | 1 | 3 | 2 (rest ✓) |
| `AO-build-deploy.md` | 11 | 0 | 0 | 0 | 2 (rest ✓) |
| `quick-wins-DM-DN-CA.md` | 8 | 0 | 5 | 1 | 1 (rest ✓) |
| **Wave 2 Total** | **~30 actionable** | **0** | **7** | **10** | **10** |

## Cumulative Wave 1 + Wave 2

| | 🔴 | 🟠 | 🟡 | 🟢 | Total actionable |
|---|---:|---:|---:|---:|---:|
| Wave 1 | 1 | 9 | 12 | 12 | ~40 |
| Wave 2 | 0 | 7 | 10 | 10 | ~30 |
| **Total** | **1** | **16** | **22** | **22** | **~70** |

## Class breakdown (Wave 2 findings)

| Class | Count | Notes |
|---|---:|---|
| A — baseline-breaker | 1 | DM-1 (Footer hardcoded status) — affects 2.3 V finding |
| B — routing/contract | 1 | C-5 (no `isAssetId` validator) |
| C — visual/layout | 5 | J-3, J-5, J-7, CA-1, CA-2 |
| D — catalog-only | 22 | most Wave 2 findings |
| E — off-band | 1 | C-17 (no CORS layer in API code) |

## Top 5 highest-severity Wave 2 findings

1. 🟠 **HIGH — C-17 (Class E):** no `tower_http::cors::CorsLayer` registered in `crates/api/src/` Router. Production deploy will reject browser requests from non-same-origin frontend unless infra layer (API Gateway / ALB / CloudFront) terminates CORS. Off-band; needs explicit confirmation from infra owner.
2. 🟠 **HIGH — DM-1 (Class A):** `libs/ui/src/layout/Footer.tsx:78-102` hardcodes green dot + "All systems operational" string with no data probe. Same anti-pattern as V live indicator → confirms pre-audit hypothesis.
3. 🟠 **HIGH — CA-1 + CA-2 (Class C):** Footer Terms/Privacy/Cookies + Resources (GitHub/Stellar docs/Soroban docs) links are dead `<span>` (no `href`) — pre-launch legal + discoverability gap, even the project's own GitHub link missing.
4. 🟠 **HIGH — DN-1 (Class D):** no build version / commit SHA in UI; post-launch debugging blocker (Vite `define` injection trivial).
5. 🟠 **HIGH — Q-4 (Class D):** task 0066 triple-drift (frontmatter `active`, body `## Status: Backlog`, history note "Implemented") + empty `related_adr` / `related_tasks` — confirms + expands Wave 1 A2.

## Blockers hit

- None. All grep / Read operations completed; no external tooling needed.
- Branch-protection check (AR-7) not verifiable locally — flagged for human GitHub-settings review.
- CORS layer (C-17) similarly may exist at infra layer outside repo — flagged as "infra confirmation needed".

## Decisions made

1. **Did not run live UI / Playwright.** Per task brief: Wave 3 is the Playwright wave; Wave 2 is grep + Read only.
2. **Treated 1.20 Footer as a Wave 2 finding even though it's "visual"** because the data layer is verifiable from grep (no fetch wired, no version stamp injected). True visual fidelity is 2.1.
3. **Did not enumerate every archived task's AC checkbox state.** Sampled 4 recent tasks (0238, 0246, 0249, 0251); pattern is consistent. Bulk verification deferred to Phase 3 if needed.
4. **Did not write a `.trash/` move-for-fix** for the trivial issues (0066 drift, footer hrefs). Per CLAUDE.md "read-only audit" stance + Wave 2 isn't a gate.

## Suggested next wave

Per task README "Execution Strategy → Waves":

- **Wave 3 (Tier 2 Playwright + grep):**
  - Playwright session: 1.7 Cross-entity links + 1.13 URL state nav + 1.14 Search (single MCP run hitting all routes)
  - Parallel Explore agent: 1.17 Security + 1.22 Polling/cache (grep)
  - 🛑 GATE A at end of Wave 3 — triage required before Wave 4

- **Ready for Wave 3? Yes.** Prerequisites:
  - Local stack (Vite :4200 + API :9000 + Postgres :5433) must be running for Playwright pass — currently uncertain; check before starting.
  - Polling/cache grep can run without stack.
  - Security subset grep can run without stack.

- **Bonus immediate items from Wave 2 worth doing before Wave 3:**
  1. Confirm CORS handling with infra owner (C-17) — single yes/no answer unblocks pre-launch.
  2. Add Vite `define: { __BUILD_SHA__: … }` (DN-1) + render in Footer — single-commit fix.
  3. Wire Footer link hrefs (CA-1, CA-2) even with `#` placeholders + `aria-disabled` — better than dead `<span>`.

  All 3 are sub-30-min fixes deferrable to Gate A spawn batch.

---

# Wave 3 — Tier 2 Playwright + grep (sequential single-agent)

Operator: karolkow (single Claude agent, sequential)
Wave: 3 (Tier 2 Playwright + grep — pre-Gate A)
Mode: read-only senior fresh-eye audit, single Playwright MCP browser session
Duration estimate: ~2.5h equivalent

## Sub-phases executed

| # | Sub-phase | Output file | Status |
|---|---|---|---|
| 1.7  | K Cross-entity link integrity | `findings/K-cross-entity-links.md` | ✓ done |
| 1.13 | E URL state nav functional | `findings/E-url-state-functional.md` | ✓ done |
| 1.14 | L Search functional | `findings/L-search-functional.md` | ✓ done |
| 1.17 | H Security subset | `findings/H-security-subset.md` | ✓ done |
| 1.22 | I Polling / cache logic | `findings/I-polling-cache.md` | ✓ done |

## Findings counts per file (Wave 3)

| File | Findings | 🔴 | 🟠 | 🟡 | 🟢 | ✓ pass |
|---|---:|---:|---:|---:|---:|---:|
| `K-cross-entity-links.md` | 6 | 0 | 3 | 1 | 2 | 0 |
| `E-url-state-functional.md` | 8 | 1 | 1 | 2 | 1 | 3 |
| `L-search-functional.md` | 6 | 0 | 1 | 2 | 0 | 3 |
| `H-security-subset.md` | 12 | 0 | 0 | 0 | 1 | 11 |
| `I-polling-cache.md` | 8 | 0 | 1 | 2 | 1 | 4 |
| **Wave 3 Total** | **40** | **1** | **6** | **7** | **5** | **21** |

(`✓ pass` = item verified-good, not a finding; the 19 actionable count
in the cumulative tally below excludes these.)

## Cumulative Wave 1 + Wave 2 + Wave 3

| | 🔴 | 🟠 | 🟡 | 🟢 | Total actionable |
|---|---:|---:|---:|---:|---:|
| Wave 1 | 1 | 9 | 12 | 12 | ~40 |
| Wave 2 | 0 | 7 | 10 | 10 | ~30 |
| Wave 3 | 1 | 6 | 7 | 5 | ~19 |
| **Total** | **2** | **22** | **29** | **27** | **~89** |

## Class breakdown (Wave 3 findings, actionable only)

| Class | Count | Notes |
|---|---:|---|
| A — baseline-breaker | 0 | (none — F-E-1 mostly invalidates state matrix but it's a Class B routing bug; downstream of fix, state matrix measures intended URL contract) |
| B — routing/contract | 10 | K-1, K-2, K-3, K-4, E-1, E-2, E-3, E-8, L-1, I-3 |
| C — visual/layout | 1 | L-2 (hint copy enumerates 4 of 6 entity types) |
| D — catalog-only | 7 | K-5, K-6, E-7, L-6, H-12, I-4, I-6, I-7 (8 items, but H-12 already de-prioritised under "naming nit") |
| E — off-band | 0 | Security baseline clean |

## Top 5 highest-severity Wave 3 findings

1. 🔴 **CRITICAL — F-E-1 (Class B):** Pagination cursor never written to URL on `/transactions` AND `/ledgers` Next-click. Wiring through `useCursorPagination` → `usePageHandlers` → `PaginationControls` is correct in source, but `window.location.search` stays empty after click. Refresh on page N drops to page 1. Defeats the entire `useTableUrlState` abstraction. Same defect almost certainly hits every list page + every multi-section detail (cursor_p / cursor_t / cursor_e / cursor_i). **Gate A fix-first candidate.**
2. 🟠 **HIGH — F-K-1 (Class B):** Tx detail stub confirmed live — `/transactions/:hash` renders only `Transaction/transactions/:hash` + raw hash. No validation; `/transactions/nothash` shows the stub with `hash=nothash`. Cascade: every Wave 4 1.5 cell for E3 (9 cells) is N/A, every cross-entity tx-hash link lands here. Wave 1 A1 stands.
3. 🟠 **HIGH — F-E-2 (Class B):** Lowercase `?op=invoke_host_function` from URL → MUI "out-of-range value" warning ×4 + API 400 + 0 rows. `normalizeOperationType` doesn't normalise URL input. Confirms Wave 2 C-2 with live evidence.
4. 🟠 **HIGH — F-K-2 + F-K-3 (Class B):** Pool detail: reserve labels for USDCOIN/EUR have no `<a href="/assets/:id">`; participants table "Since ledger" cells render plain `1,019` / `1,021` with no `<a href="/ledgers/X">`. Both are the standard cross-entity link pattern broken on one page.
5. 🟠 **HIGH — F-L-1 (Class B):** Liquidity-pool strkey (`L...`) entered into search returns 0 results across all 6 tabs, and the empty-state hint omits the `L...` prefix from supported formats. Pool's user-facing identifier is a dead identifier in search flow. Same root cause as K-4 strkey↔hex inconsistency.

## Gate A triage candidates list (most likely fix-first before Wave 4)

| Finding | Class | Why fix-first |
|---|---|---|
| F-E-1 (URL cursor write fails) | B | If cursor never persists, Wave 4 state matrix 1.5 cells D2/D9 can't distinguish "page 1 only" from "polling stale" — measurement noise. Cascade to all 14 list/multi-section routes. |
| F-K-1 + A1 (tx detail stub) | B | 9 state-matrix cells N/A for E3 + every cross-entity link unreachable. Decide: implement (0070+0071), or document "Wave 4 baseline: E3 = stub" and skip 9 cells. Per task README Gate A item #1 — explicit per-stub decision required. |
| F-E-2 (lowercase op filter) | B | If lowercase URL → API 400, Wave 4 1.6 console will register API 400 for every list page Next-click that re-applies a lowercase filter. Either fix-first (normalise) or document "audit-baseline assumption: filter values uppercase only". |
| F-K-2 + F-K-3 (pool detail link gaps) | B | Wave 6 Track 2 Playwright pass will re-report these per-page. Fix-first means Track 2 doesn't duplicate. |
| F-L-1 (pool strkey search) | B | Same strkey↔hex root cause as K-4. Spawning one shared "strkey canonicalisation" task closes K-4 + L-1 together. |

**Cumulative Gate A items:** add to the Wave 1 + Wave 2 candidates
(A1 tx stub already there; DM-1 footer hardcoded; CA-1 / CA-2 footer
dead links; C-17 CORS confirm; DN-1 build SHA; the Vite 7.3.1 → 7.3.3
CVE bump).

## Blockers hit

- None hard. Playwright `browser_click` against e162 (operation
  combobox) timed out because of a decorative SVG `<ellipse>`
  intercepting pointer events. Worked around by direct URL navigation
  (`?op=INVOKE_HOST_FUNCTION`) — confirmed filter applies + URL
  preserves on refresh. Real Playwright click on the Next button
  worked once given a `data-test-id` selector.
- Initial dispatched-MouseEvent failed to trigger MUI handler — used
  proper Playwright click after stamping `data-test-id`. Captured as a
  test-ergonomics nit (Wave 6 Track 2 will need data-test attributes
  anyway for CLI Playwright per `feedback_playwright_mcp_vs_cli`).

## Decisions made

1. **Did NOT spawn fix-first tasks.** Per task brief "Read-only audit
   (with triage gates)" + this wave is the Gate A entry, not the gate
   itself. Triage decisions go in `findings/triage-gate-A.md` (user
   step). Then Gate A fix-first tasks spawn from those decisions.
2. **Tested URL-state via direct navigation rather than UI clicks** to
   sidestep the decorative-SVG pointer-event interceptor. Same
   outcomes either way for URL contract checks.
3. **Did not exercise top-nav search textbox separately** — it feeds
   `/search?q=` via the same `useSearchResults` hook, so direct URL
   navigation is equivalent. Live keyboard typing into the textbox
   would be needed for debounce timing measurement, which Wave 6 will
   cover.
4. **Skipped Class E off-band action** — security baseline is clean,
   no CVE in Wave 3 (Vite CVE was Wave 1 F-CO-1, separately tracked).
5. **F-K-4 (pool URL hex vs strkey)** could be elevated to Class A
   (baseline-breaker for Wave 4) but kept Class B because the routes
   resolve correctly when given hex; only the user-input strkey path
   breaks. Per task taxonomy Class B is the right fit (routing
   contract issue, not a downstream-measurement invalidator).

## Suggested next step

🛑 **GATE A** — task brief mandates explicit triage before Wave 4.

Recommended Gate A artifact: `findings/triage-gate-A.md` with these
section headings:

1. **Stub decisions** — per-page (E3 tx detail definitely; E9 contract
   tabs may already be functional, recheck) → fix-first or
   "Wave 4 baseline: STUB" written down.
2. **URL contract** — accept F-E-1 fix as Wave-4 blocker, since
   leaving it broken makes state matrix readings unreliable.
3. **Case normalisation** — F-E-2 fix decision: normalise at URL
   parse, normalise on Select change, or reject + clear param.
4. **Strkey canonicalisation** — F-K-4 / F-L-1 single shared task.
5. **Cross-entity link gaps** — F-K-2 / F-K-3 fix-first (small).
6. **Off-band CVE** — Wave 1 Vite bump still pending if not done.
7. **Defer to Phase 3 (Class D)** — H-12 storage key naming, I-4 dead
   `invalidateResource`, I-6 `refetchIntervalInBackground` pin, I-7
   `detailPolicy.gcTime` bump, K-5 self-link, K-6 column-completeness
   matrix nit, E-7 tab URL state, E-8 cursor_*.

After Gate A fix-first lands: rebase
`research/0257_frontend-comprehensive-audit` onto develop, snapshot
new baseline in worklog, proceed to Wave 4 (1.5 state matrix +
1.6 console + 1.9 + 1.9b + 1.15 + 1.12).


---

## Gate A — triage decisions written

**Time:** end of Wave 3, before Wave 4.
**Output:** `findings/triage-gate-A.md` (read-only decision doc).

**Result summary:**
- 5 Class A (baseline-breaker) → 0 fix-first / 3 defer Gate B / 2 defer Phase 3
- 9 Class B → **3 fix-first** (F-E-1 cursor URL, F-E-2 op case normalise, F-K-1+A1 TxDetail decision) / 6 defer Gate B
- 5 Class C → all defer Gate B
- ~22 Class D → all defer Phase 3 bulk-spawn (3.2)
- 2 Class E → off-band immediate (Vite 7.3.3 CVE bump, CORS infra question)

**Gate A blockers identified for Wave 4:**
1. F-E-1 cursor write → spawn audit-blocker, fix before Wave 4
2. F-E-2 op case → spawn audit-blocker, fix before Wave 4
3. F-K-1 + A1 TxDetail stub → user decision required (implement vs document baseline). Default = document baseline.

**Off-band:**
- Vite 7.3.3 CVE bump — does not block Wave 4
- CORS — route to backend/infra owner

**Awaiting user signal** before spawning fix-first backlog tasks.

## Gate A — E3 decision RESOLVED

**Per user 2026-05-25:** 0070 + 0071 (TxDetail normal + advanced) in-flight by FilipDz on local branches, not pushed to origin (verified via `git ls-remote` — zero matching refs).

**Decision:** Path B (document baseline + delta-audit). NO audit-blocker spawn for E3 (would duplicate Filip WIP, risk merge conflict).

**Wave 4 plan adjustment:**
- E3 row pre-marked `N/A — gated on FilipDz 0070+0071 merge` in 1.5/1.6/2.0/2.1/2.4 outputs
- Effective matrix: 1.5 = 13×9 = 117 measurable cells (was 126); 2.4 = 13×3 = 39 (was 42)
- Delta-audit post-merge ~1.5h scope: full E3 sweep (1.5 + 1.6 + 1.7 from-E3 + 2.0 + 2.1 + 2.4)

**Action item for user:** ask Filip ETA on 0070+0071 push/merge. If <1 week, queue delta. If longer, document in audit-summary.md as "E3 row not covered, gated on Phase Y".

**Net Gate A fix-first scope reduced to 2 audit-blocker tasks:**
1. F-E-1 (URL cursor write)
2. F-E-2 (lowercase op normalise)

Plus 1 off-band (Vite 7.3.3 CVE bump, non-blocking) + 1 communication (CORS infra question).

## Post-merge analysis — develop @ 6b7fb558 (FilipDz tx-detail PR #215)

Branch `research/0257_frontend-comprehensive-audit` just merged
`origin/develop` (merge commit `6b7fb558`). The substantive FE delta is
PR #215 (merge commit `a2c1b205`, `feat(lore-0070): add transaction
detail page`).

Scope: 33 FE files / +2124 / −213 LOC. New dir
`web/src/pages/transaction-detail/` (20+ files, 1799 LOC) + layout
refactors in `libs/ui/src/layout/{Footer,TopNav,SecondaryNav}.tsx` +
new `libs/ui/src/theme/grid.js` + theme overrides edits + small edits
to `web/src/pages/TransactionsListPage.tsx`,
`web/src/pages/transactions/TransactionFilters.tsx`,
`web/src/router/AppShell.tsx`, `web/src/pages/detail/SectionCard.tsx`,
`web/src/pages/detail/SummaryRow.tsx`, plus NEW
`tools/dev-mock-api.mjs` (467 LOC, dev-only mock).

**No new package.json deps.**

### Resolution pass

| Finding | Pre-merge state | Post-merge verdict | Citation |
|---|---|---|---|
| **A1 archaeology** 🔴 TxDetail stub | 14-line `PageStub` | **RESOLVED** in `a2c1b205` | `web/src/pages/transaction-detail/index.tsx` (145 LOC + 19 supporting files) |
| **F-K-1** Wave 3 stub confirm | Playwright confirmed stub | **RESOLVED** — real page, valid hash → 4 sections; invalid hash → `NotFoundState`; error → `GenericErrorState` w/ retry | `useTxHashParam.ts:9-12`, `index.tsx:33-73` |
| **DM-1** Footer "All systems operational" hardcoded | Hardcoded literal | **STILL STANDS** — Filip refactor was layout-only | `Footer.tsx:114-116` (unchanged data path) |
| **CA-1** Footer legal dead `<span>` | No `href` | **STILL STANDS** | `Footer.tsx:25-29` (LEGAL array unchanged) |
| **CA-2** Footer resources dead `<span>` | No `href` | **STILL STANDS** | `Footer.tsx:18-23` (RESOURCES array unchanged) |
| **J-3** TopNav `formatNumber` duplicate | Local re-impl | **STILL STANDS** | `TopNav.tsx:78-84` (unchanged) |
| **J-4** STROOPS_PER_XLM single site | 1 site, drift risk | **DEGRADED** — bumped 🟡 → 🟠. Second copy added in `transaction-detail/shared/formatFee.ts:3` (BigInt variant) | finding J-4 update |
| **J-5** absolute UTC inconsistency | Tx page only had it | **PARTIALLY RESOLVED + new dup**. Tx detail surfaces absolute UTC, but via local re-impl | `TransactionSummary.tsx:62-73` |
| **J-7** local truncation re-impls | 2 sites | **DEGRADED** 🟡 → 🟠. Now 6 sites: +shortHash, +shortId(2nd), +shortenStrKey, +truncateHex | finding J-7 update |
| **A2** 0066 task body drift | Lore drift | STILL STANDS | n/a |
| **A3** 25/28 FW gap | 82% gap | STILL STANDS (0070/0071 now ✓ but 23 others remain) | n/a |
| **A4** 0226 test infra blocked | 10-day stall | STILL STANDS | n/a |
| **A5** 0199/0215 LP blocked | Oracle ADR pending | STILL STANDS | n/a |
| **F-AI-1** Main bundle 594KB | Above Vite warning | STILL STANDS (596.20 KB / 189.36 KB gz post-merge; +1.65 KB raw / +0.49 KB gz) | bundle delta table |
| **F-AI-2** LP chunk 313KB | Heavy chart | STILL STANDS (307.12 KB — slightly smaller) | bundle delta table |
| **F-P-1** lint warning assetColor:131 | 1 warning | STILL STANDS, baseline unchanged (0 errors / 1 warning) | nx lint post-merge |
| **F-AQ-1** noUncheckedIndexedAccess off | Flag disabled | STILL STANDS | tsconfig unchanged |
| **F-AQ-4** No branded IDs | Zero hits | DEGRADED (surface +) | new tx-detail strings unbranded |
| **F-AF-1** Error interceptor flattens | Information loss | STILL STANDS | client.ts unchanged |
| **search → E3 redirect** Wave 3 L matrix | Stub landing | RESOLVED, lands at real page | n/a |

### New findings introduced by Filip's PR

| ID | Severity | Class | One-liner |
|---|---|---|---|
| **F-J-16** | 🟠 HIGH | C | Duplicate `formatFee` function (BigInt-based) in `transaction-detail/shared/formatFee.ts:5-13` collides with `transactions/formatters.ts` `formatFee` (Number-based) |
| **F-J-17** | 🟡 MEDIUM | C | `formatStroops` 3rd entry point for stroop display naming convention |
| **F-K-7** | 🟡 MEDIUM | B | E3 ledger sequence cell needs Playwright verify for `<a href>` to `/ledgers/:seq` |
| **F-K-8** | 🟡 MEDIUM | C | Soroban call tree `destination_account` routing needs Playwright verify for clickable link |
| **F-AI-10** | 🟡 MEDIUM | B | New `TransactionDetailPage-*.js` chunk = 29.97 KB / 9.13 KB gz; introduces `ToggleButtonGroup` + `KeyboardArrowDown` shared chunks |
| **F-AI-11** | 🟢 LOW | D | `TransactionsListPage-*.js` grew +0.15 KB raw (informational) |
| **F-AQ-7** | 🟡 MEDIUM | B | `unknown` + runtime shape-probes for heavy XDR `details` field — correct defensive pattern, but signals OpenAPI gap (no discriminated union by `op_type`) |
| **F-AQ-8** | 🟡 MEDIUM | C | Triple cast `(heavy as { results_meta_xdr?: ... })` in `index.tsx:131-138` — `results_meta_xdr` exists at runtime but not in generated TS shape (api-types codegen gap) |

### Re-audit queue (Wave 4 delta scope)

| Sub-phase | Scope on E3 / Filip files | Effort | Priority |
|---|---|---:|---|
| **1.1 OpenAPI adherence** | Verify `useTransactionDetail` uses `getTransactionOptions` (✓ confirmed); audit `results_meta_xdr` codegen gap (F-AQ-8) | 10 min | Wave 4 |
| **1.5 state matrix E3 row** | 9 cells: loading / success / empty-ops / empty-events / empty-sigs / not-found / 4xx error / 5xx error / 404 invalid-hash | 30 min Playwright | **Wave 4 BLOCKER** |
| **1.6 console + error handling E3** | Inspect invalid-hash, 404, 500 paths for console.error; verify `SectionErrorBoundary` does not leak `console.error` past prod-guard | 15 min | Wave 4 |
| **1.7 cross-entity links FROM E3** | Outbound: ops table → contracts / accounts (✓ via `IdentifierWithCopy`); breadcrumb → /transactions; ledger cell → /ledgers/:seq (F-K-7); flow tree destination → /accounts/:id (F-K-8) | 20 min | Wave 4 |
| **1.8 data formatting** | New formatters (humanizeOp, formatFee BigInt, describeMemo, formatStroops, formatUtcAbsolute) — count toward J-3 / J-4 / J-5 / J-7 (already updated above) | 0 (done in this pass) | done |
| **1.9 component reuse** | 10 `from '@rumblefish/soroban-block-explorer-ui'` imports across tx-detail files. Does NOT use `ExplorerTable` — implements its own MUI `<Table>` in `EventsSection` + `SignaturesTable`. Uses `SectionCard` (from `web/src/pages/detail/SectionCard.tsx` — page-local, not libs/ui) | 20 min audit | Wave 4 |
| **1.9b coupling** | New tx-detail dir structure: `normal/` / `advanced/` / `sections/` / `shared/`. `index.tsx` imports across all 4 subdirs. Boundary: `sections/` = mode-shared, `normal/+advanced/` = mode-specific, `shared/` = pure utils. Clean | 15 min | Wave 4 |
| **1.10 senior craft** | `useDetailMode` URL state (`?mode=advanced`, replace-not-push, drops param on normal — F-E-3-aligned), `useTxHashParam` (validator + return shape), `OperationPicker` (sticky-selected + filter chips), `ModeToggle` Figma-aligned | 25 min | Wave 4 |
| **1.11 P code quality** | Re-ran in this pass — 0 new errors, 0 new warnings (baseline holds) | 0 (done) | done |
| **1.11b AQ type safety** | F-AQ-3 (2 new switches, neither uses `assertNever`), F-AQ-7 (unknown probes), F-AQ-8 (codegen gap) added | 0 (done) | done |
| **1.12 state separation** | `useDetailMode` is URL state, not `useTableUrlState` (no cursor / no sort) — different pattern. Acceptable: tx-detail has no pagination. Verify against 1.12 EXTRA finding "useTableUrlState justification" | 10 min | Wave 4 |
| **1.13 URL state nav** | `?mode=advanced` persists across refresh, `?mode=normal` drops the param (clean default). Verify back-button behaviour | 10 min Playwright | Wave 4 |
| **1.15 Stellar domain** | XdrRow (base64 expander + copy), HighlightedJson (typed JSON render — string/number/bool/null/key tokens), strkey on contract_id (5+4 truncation), TX hash full-display via `IdentifierWithCopy truncate={false}`. Op type mapping via `formatOperationType` — reused from `transactions/operationTypes`. SHOULD verify Soroban `INVOKE_HOST_FUNCTION` invocation recursion is rendered correctly + network passphrase still implicit (Mainnet-only build today) | 30 min | Wave 4 |
| **1.16 bundle analysis** | Re-ran in this pass (+29 KB E3 chunk, +0.5 KB gz main creep). Captured in F-AI-10 / F-AI-11 | 0 (done) | done |
| **1.17 security** | Re-ran greps in this pass — 0 hits for `dangerouslySetInnerHTML`, `innerHTML =`, `eval(`. `HighlightedJson` walks objects + emits text nodes inside `<Typography component="pre">` (React JSX text escaping). `XdrRow` renders raw base64 string inside `<Typography component="pre">` (text-only). `writeClipboard` uses `navigator.clipboard.writeText` (no innerHTML). Safe. | 0 (done) | done |
| **1.20 quick wins** | `tools/dev-mock-api.mjs` is dev-only tooling; gated on whether it matches OpenAPI. Phase 3 scope per original plan | 0 (defer) | Phase 3 |

**Wave 4 delta-audit total effort estimate: ~3 hours** (vs Wave 4 baseline
of ~6-8h that was already planned). Most expensive items: 1.5 (30min),
1.15 (30min), 1.10 (25min), 1.7 (20min), 1.9 (20min).

### Deterministic delta checks done this pass

| Check | Result |
|---|---|
| **`nx run-many -t typecheck`** | exit 0, "Successfully ran target typecheck for 4 projects and 2 tasks they depend on" — 0 new errors (Wave 1 baseline: 0 errors) |
| **`nx run-many -t lint`** | exit 0, "1 problem (0 errors, 1 warning)" — same warning as Wave 1 baseline (assetColor.ts:131) |
| **`nx build @rumblefish/soroban-block-explorer-web`** | exit 0, 2.40s. main = 596.20KB / 189.36KB gz (+1.65/+0.49 vs baseline); LP detail = 307.12KB / 93.81KB gz (−5.99/−1.76 vs baseline); E3 = 29.97KB / 9.13KB gz (was 0.95KB stub) |
| **`grep -rnE "fetch\(|axios" web/src/pages/transaction-detail/`** | 1 hit (`index.tsx:69`: `query.refetch()` from TanStack — not raw `fetch`). Generated client used via `getTransactionOptions` → `useTransactionDetail.ts:7-11` |
| **`grep -rnE "dangerouslySetInnerHTML|innerHTML\s*=|eval\("`** in tx-detail + OperationFlowTree | **0 hits**. XSS surface clean |
| **`grep -rnE "toLocaleString|toFixed|/ 1e7|Number\("` in tx-detail** | 2 hits in `OperationPicker.tsx:21,182` — `opNumber` arithmetic, no formatter dup. Other formatters now live in dedicated `shared/formatFee.ts` + `sections/TransactionSummary.tsx:62-73`. See J-4, J-5, F-J-16 in J-data-formatting.md |
| **libs/ui import count from tx-detail** | 10 imports across 9 files (`Chip` x4, `IdentifierDisplay`, `IdentifierWithCopy`, `RelativeTimestamp`, `OperationFlowTree`, `CopyButton`, `monoFontFamily`, `isTransactionHash`, `classifyError`, `NotFoundState`, `GenericErrorState`, `CardSkeleton`, `SectionErrorBoundary`, `FlowNode` type). Healthy reuse. **Does NOT use `ExplorerTable`** — `EventsSection` + `SignaturesTable` implement their own MUI `<Table>` (deliberate — column shapes diverge from list-page table contract) |

### Net change to audit plan

- **Wave 4 NOT blocked.** E3 row no longer N/A; matrix restored to full
  14×9 = 126 cells (1.5) and 14×3 = 42 cells (2.4).
- **Gate A fix-first scope unchanged:** still 2 audit-blocker tasks
  (F-E-1, F-E-2) + 1 off-band (Vite CVE) + 1 comm (CORS).
- **Severity escalations queued for Gate B:** J-4 🟡→🟠, J-7 🟡→🟠.
- **8 new findings** introduced by Filip's code, all Class B/C/D
  (deferrable to Gate B / Phase 3). None of them block Wave 4 start.
- **Delta-audit work ~3 hours** folds naturally into Wave 4 sub-phases —
  no separate session required if Wave 4 absorbs the delta queue above.
- **Recommendation: start Wave 4 immediately** with the E3 delta-audit
  queue threaded into each sub-phase. The 3h delta is dwarfed by Wave 4's
  baseline ~6-8h budget.
- **Followup spawn candidates from new findings** (for Phase 3 sub-phase
  3.2 bulk-spawn, none urgent):
  - F-J-16 (duplicate formatFee) — consolidate into one helper in libs/ui
  - F-AQ-8 (codegen gap on `results_meta_xdr`) — backend OpenAPI schema fix
  - F-AQ-7 (unknown probes for heavy details) — OpenAPI discriminated union
    by `op_type` (cross-refs 0075 future-work)
  - F-K-7, F-K-8 — verify via Playwright in Wave 4; fix if dead

---

## Post-merge analysis — develop + 0254 @ 6af74d82 (Karol prev_cursor)

Scope: 40 files / +2103 / −632 across backend (handlers / queries /
cursor / pagination / openapi schemas / +544 LOC integration tests) +
FE primitives (`useCursorPagination`, `usePageHandlers`) + 5 list
pages + 8 tab tables + api-types regen + ADR 0008 amendment.

Pre-merge develop tip pulled in `68b40058` brought only Staś's
spawned `0261_BUG_parser-missing-pool-id-on-path-payment-ops.md`
(lore-only, 0 code impact).

### Resolution pass

| Finding | State | Evidence |
|---|---|---|
| **F-E-1** 🔴 URL cursor not written | **RESOLVED** in `f646047d` + `78345d49` (merged via `6af74d82`) | `useCursorPagination.ts:91-103` both directions call `setCursor` → `useTableUrlState.ts:91-99` calls `setParams(...,{replace:true})` writing `?cursor=` to URL. Pre-merge root cause = wiring (list pages called `onPrev={goPrev}` direct stack-pop, never URL write); 0254 dropped FE prev-stack entirely. |
| **F-E-2** 🟠 lowercase op normalise | STILL STANDS | 0254 did not touch `operationTypes.ts` or `TransactionFilters.tsx`. The 13-line diff in `TransactionsListPage.tsx` is pagination-only. |
| **F-E-3** 🟡 catch-all 404 no `<main>` | STILL STANDS | unrelated |
| **F-E-7** 🟡 no URL state for tabs | STILL STANDS | unrelated |
| **F-E-8** 🟢 `cursor_p/_t/_e/_i` keys also missing from URL | **RESOLVED** via same fix | same `setCursor` write path, just different `cursorParam`. |
| **F-K-2 + F-K-3** 🟠 pool detail link gaps | STILL STANDS | `PoolParticipants.tsx` + `PoolTransactions.tsx` only changed pagination wiring; row link rendering unchanged. |
| **F-AF-1** 🟡 client.ts error flatten | STILL STANDS | `client.ts` untouched. `ErrorEnvelope` shape unchanged in api-types regen (only `PageInfo` mutated). |
| **F-AQ-1, F-AQ-4, F-AQ-7, F-AQ-8** type safety | STILL STANDS | `tsconfig` unchanged; no new branded IDs; no new `unknown` casts in touched files. |
| **F-I-3** polling cache | STILL STANDS | No TanStack dedup behaviour change — `useCursorPagination` still pairs with `useQuery` per distinct cursor key. |
| **F-J-16** duplicate formatFee | STILL STANDS | unrelated |
| **F-CO-1** Vite 7.3.3 CVE | STILL STANDS | unrelated |
| **F-AI-1, F-AI-2, F-AI-10** bundle | STILL STANDS at same sizes (see deltas below) | re-measured |

### Re-audit queue (folded into Wave 4)

| Sub-phase | Scope | Effort | Priority |
|---|---|---:|---|
| 1.5 D2 / D9 for E2 / E4 / E7 / E10 / E12 (list pages) | Re-verify Next + Prev + refresh + deep-link on 5 list pages | 30 min Playwright | Wave 4 |
| 1.5 D2 / D9 for E6 / E8 / E9 / E11 / E13 (tab tables w/ namespaced cursors) | Same on `cursor_p` / `cursor_e` / `cursor_i` per-section cursors | 25 min Playwright | Wave 4 |
| 1.9 component reuse | Quick sanity on `usePageHandlers` extracted chunk (now shared across 13 pages — uniform pattern, slight reuse improvement) | 5 min | Wave 4 |
| 1.18 Q+AR lore | Check 0254 task close metadata, ADR 0008 amendment cross-refs | 10 min | Wave 4 |
| **Total** | | **~70 min** | absorbed into Wave 4 baseline |

### Deterministic delta done this pass

| Check | Result |
|---|---|
| `nx run-many -t typecheck` | exit 0, 0 errors. `cursor` → `next_cursor` rename caught zero consumer drift. |
| `nx run-many -t lint` | 1 problem (0 errors, 1 warning), same `assetColor.ts:131` baseline |
| `nx build web` | exit 0, 2.23s. main = 596.20 KB / 189.37 KB gz (+0 raw / +0.01 KB gz noise). LP detail = 307.15 KB / 93.81 KB gz (negligible). E3 = 29.97 KB / 9.13 KB gz (unchanged). NEW shared chunk: `usePageHandlers-*.js` = 2.35 KB / 1.16 KB gz. |
| `grep "\.cursor\b"` in `web/src`+`libs/ui/src` | 2 hits, both `state.cursor` URL-local cursor — intentional. Zero stale `page.cursor` refs. |
| `grep "\.has_more\b"` | 0 hits — clean rename. |
| `grep "\.next_cursor\b\|\.prev_cursor\b" libs/ui` | 4 hits, all in `usePageHandlers.ts:47-50` (single consumer of the new wire shape). |
| `grep "as any\|@ts-ignore\|@ts-expect-error"` in touched files | 0 hits |
| `grep "fetch(\|axios"` in touched files | 0 raw — only TanStack `refetch()` (false positive) |

### Net change to audit plan + Gate A

- **Gate A fix-first scope reduced from 2 → 1.** Only F-E-2 (lowercase
  op normalise) remains as audit-blocker. F-E-1 RESOLVED in merge.
- **`lore/1-tasks/backlog/0261_BUG_url-cursor-not-written.md` is
  OBSOLETE pending user signal** to delete or rename. NOT deleted by
  this analysis pass — user owns the call.
- **Filename ID collision flag:** `0261_BUG_url-cursor-not-written.md`
  and Staś's `0261_BUG_parser-missing-pool-id-on-path-payment-ops.md`
  share `0261_` prefix at filesystem level. Two files with same ID
  prefix. User decision (renumber, or accept since the cursor task is
  obsolete anyway).
- **Wave 4 unblocked.** F-E-1 was the worst blocker; F-E-2 is
  single-file (`operationTypes.ts`) — can either spawn as audit-blocker
  (preserve clean baseline) or absorb as known noise during 1.6
  console review.
- **Audit branch tip:** `6af74d82` is the Wave 4 baseline SHA. Pin it
  to worklog.
- **Severity escalations from 0254:** **none** — every new finding is
  Class B/C deferrable to Gate B.

### New findings introduced by 0254

**None of meaningful severity.** The merge resolved a 🔴 CRITICAL
finding (F-E-1) + a 🟢 LOW (F-E-8) without introducing any new
audit-visible regressions. The `cursor` → `next_cursor` BREAKING
rename was disciplined: api-types regen + all FE consumers updated in
the same merge; tsc green. The new shared `usePageHandlers-*.js`
chunk is a marginal reuse improvement, not a finding.

(One process-flavoured observation worth noting for 1.18: 0254 task
close references "defer test suite to 0257" in commit `14ee5852`.
0257 scope inherits **no backend test work** — it inherits the
verified-via-backend-tests FE wire contract. Quick verify-in-1.18 that
this forward-link doesn't bleed task scope.)

### Recommendation

**Wave 4 starts immediately** with the delta-audit queue above
threaded into each sub-phase. ~70 min of extra scope vs Wave 4's
6-8h baseline. F-E-2 audit-blocker can spawn in parallel (~15 min code
fix); if not, absorb the API 400 noise as a known-baseline tag during
1.6 console review.

## Gate A — F-E-2 DROPPED (design decision)

**Per user 2026-05-25:** F-E-2 (lowercase op filter case normalisation)
dropped as fix-first AND as audit finding. Senior design call:

> "URL to URL i po prostu powinien być poprawny i tyle."

Re-classified ACCEPT BASELINE. URL = wire contract. FE owns
canonicalisation only for URLs it *produces*. Non-canonical external
input → API 400 = expected REST behavior.

**Task file `0262_BUG_url-op-filter-case-normalise.md` moved to
`.trash/0262_BUG_url-op-filter-case-normalise.md.DROPPED-2026-05-25-design-decision`.**

**Gate A fix-first scope: 2 → 1 → 0.** Both audit-blockers resolved or
dropped:
- F-E-1 resolved by 0254 merge
- F-E-2 dropped by design decision

**Wave 4 UNBLOCKED.** No fix-first dependencies. Audit baseline = current
`6af74d82` merge tip. Ready to start.

**Implication for 1.6 console:** when Playwright marathon hits malformed
`?op=` paste scenarios, record baseline note ("intentional REST behavior,
not FE bug"). Do NOT count as console finding.

**Surviving symptoms** (separate from F-E-2 drop):
- MUI Select "non-existent value" warning on bad input — 🟢 LOW finding for
  Wave 4 1.6 (could be solved by Select showing "Unknown filter" empty
  state instead of warning).
- Empty table without affordance on bad input — Wave 6 visual UX nit
  (consider "Invalid filter — try clearing" message).
Both are about **render quality on bad input**, orthogonal to URL
normalisation.

---

## Wave 4 — Tier 3 sequential

Operator: karolkow (single Claude agent, sequential)
Wave: 4 (Tier 3 sequential, ~10h equivalent compressed)
Baseline: audit branch tip `6af74d82` (post-0254 + post-FilipDz)
Mode: read-only senior fresh-eye audit + light Playwright marathon

### Sub-phases executed

| # | Sub-phase | Output file | Status |
|---|---|---|---|
| 1.5 | State coverage matrix 14×9=126 | `findings/D-state-coverage-matrix.csv` + `.md` companion | ✓ done |
| 1.6 | Console + error handling | `findings/M-AE-console-error-handling.md` | ✓ done |
| 1.9 | U Component reuse | `findings/U-component-reuse.md` | ✓ done |
| 1.9b | X Coupling/decoupling | `findings/X-coupling.md` | ✓ done |
| 1.12 | State separation + EXTRA useTableUrlState analysis | `findings/AL-state-separation.md` | ✓ done |
| 1.15 | Stellar domain consistency | `findings/AN-stellar-domain.md` | ✓ done |

### Findings counts per file

| File | Findings | 🔴 | 🟠 | 🟡 | 🟢 |
|---|---:|---:|---:|---:|---:|
| `D-state-coverage-matrix.csv/md` | 5 (1 🔴 cascade across 5 cells + 4 patterns) | 1 | 1 | 3 | 0 |
| `M-AE-console-error-handling.md` | 7 | 0 | 2 | 2 | 3 |
| `U-component-reuse.md` | 5 | 0 | 2 | 3 | 0 |
| `X-coupling.md` | 5 | 0 | 0 | 3 | 2 |
| `AL-state-separation.md` | 2 + EXTRA verdict | 0 | 0 | 1 | 1 |
| `AN-stellar-domain.md` | 7 | 0 | 1 | 3 | 3 |
| **Wave 4 total** | **~31 actionable** | **1** | **6** | **15** | **9** |

### Top Wave 4 finding

**F-D-1 [🔴 CRITICAL]** — Live dev API serves pre-0254 pagination shape
(`page.cursor` + `has_more`) while FE expects post-0254 shape
(`page.next_cursor` + `prev_cursor`). Result: Next + Previous buttons
**both disabled** on every list page + every tab table despite
has_more=true. Verified by `curl http://localhost:9000/v1/transactions`
and browser inspection of `/transactions` and `/ledgers`.

**Root cause:** API binary in dev not rebuilt after 0254 merge.
**Not a code bug** — deployment / dev-env drift. Production build with
rebuilt API would work.

**Phase 3 follow-ups:**
- `XXXX_CHORE_dev-env-rebuild-runbook` — document rebuild step
- `XXXX_FEATURE_api-shape-runtime-probe` — FE detects old shape and
  surfaces banner instead of silent broken pagination

### Other notable Wave 4 findings

- **F-D-2 / F-AE-5 [🟠]** Composite NotFound + TransientErrorState on
  E6/E8/E9 detail pages (account/asset/contract) — sub-section queries
  fire alongside parent 404 producing dual error blocks. Worst case
  E9 contract NotFound shows 4 error blocks. Gate B fix-first candidate.
- **F-D-3 [🟡]** Detail-page H1 inconsistency — 3 different patterns
  across 7 detail pages (generic / formatted / identifier).
- **F-AE-3 [🟡]** SectionErrorBoundary used on only 2/7 detail pages.
- **F-U-1 [🟡]** SectionCard is local impl at `web/src/pages/detail/`,
  not hoisted to `libs/ui` despite 16 consumers.
- **F-U-3 [🟠]** 6 ad-hoc truncation re-implementations (escalates J-7).
- **F-U-4 / F-AN-7 [🟠]** Two STROOPS_PER_XLM constants
  (number + bigint) (escalates J-4).
- **F-X-1 [🟡]** `assetLegLabel` reaches across folders (liquidity-pools
  → pool-detail).

### useTableUrlState verdict (EXTRA)

**KEEP as-is.** 127-line abstraction that centralizes URL ↔ typed
state, the "filter change drops cursor" invariant, reference stability,
and the multi-section cursor key convention (`cursor_p`/`cursor_t`).
Alternative (inlined `useSearchParams` per page) = ~250-400 LOC of
duplicated subtly-different logic. Lock-in minimal (1-file abstraction).
TanStack has no native URL persistence so the choice is "useTableUrlState
vs raw useSearchParams" — the abstraction wins. Full rationale in
`findings/AL-state-separation.md` Part 2.

### Blockers hit

- Nx graph CLI not available locally (`timeout: command not found`) — fell
  back to grep directionality verification for coupling check; sufficient
  but not rigorous cycle proof.
- D6/D7/D8 cells (transient / rate limit / CORS) marked `?` skipped
  across all 14 endpoints — would need MSW or Playwright network mock
  setup, out of scope. Phase 3 follow-up:
  `XXXX_FEATURE_e2e-error-injection-playwright`.

### Time budget

Estimated equivalent ~10h compressed to ~2h via Playwright batch +
grep-driven evidence + targeted spot-check rather than per-cell marathon
(126 cells × 3 min ≠ session budget). Coverage breadth preserved by
relying on shared root causes (F-D-1 explains 5 ✗ broken cells; F-D-2
explains 3 ✗ composite-NotFound cells; D6/D7/D8 skip-marked uniformly).

### Cumulative tally across Waves 1-4

| Wave | Findings | 🔴 | 🟠 | 🟡 | 🟢 |
|---|---:|---:|---:|---:|---:|
| Wave 1 (Tier 1) | ~40 | 1 | 9 | 12 | 12 (+ 6 ✓) |
| Wave 2 (Tier 1+2 grep) | ~25 | 0 | 8 | 9 | 8 |
| Wave 3 (Tier 2 Playwright+grep) | ~28 | 1 | 7 | 12 | 8 |
| Post-merge delta (Filip + 0254) | +5 escalations | 0 | +3 | +2 | 0 |
| Wave 4 (Tier 3 sequential) | ~31 | 1 | 6 | 15 | 9 |
| **Cumulative** | **~125 actionable** | **3** | **30** | **48** | **37** |

Note: cumulative 🔴 = 3 (1 from Wave 1 archaeology critical flag, 1 from
Wave 3 pagination pre-merge, 1 NEW Wave 4 F-D-1 API shape drift).
Wave 3 🔴 (F-E-1 cursor URL) RESOLVED by 0254 merge — replaced by F-D-1
which is **same surface, different root cause** (deployment drift, not
code bug).

### Readiness for Wave 5

**Ready.** Wave 4 outputs complete. Recommendation per task README
"Model tier — when to upgrade" section: **switch to `claude-opus-4-7`
extra-high (1M context) before Wave 5.** Wave 5 = Tier 4 subjective
drafts (1.2 spec/source consistency + 1.3 file/folder + 1.9c simplicity
+ 1.10/10b/10c/10d senior craft) — requires holding spec + Figma + code
+ all ~125 prior findings simultaneously for cross-finding synthesis.

---

## Wave 4 delta re-run after API binary restart (2026-05-25T14:19-14:23Z)

**Trigger:** parent agent restarted API binary from the audit branch
tip `6af74d82` (includes 0254 merge). Probe confirmed new pagination
shape `page: { next_cursor, prev_cursor, limit }`.

**Scope:** D2/D9 pagination cells across 5 list pages + 3 representative
tab-table sections. Read-only Playwright via MCP. No code changes.

### Outcomes

| Action | Result |
|---|---|
| Cells flipped ✗ → ✓ | 5 (E2/D9, E4/D9, E7/D9, E10/D9, E12/D9) |
| Cells still ✗ | 0 |
| Cells still ⚠ | 18 (unchanged) |
| NEW findings | 0 |
| Findings RESOLVED | F-D-1 (matrix), F-AE-6 (M-AE — partial: paginiation behavior resolved, "no console signal on shape drift" angle retained as 🟡) |
| Findings still standing | F-D-2, F-D-3, F-D-4, F-D-5, F-AE-1, F-AE-2, F-AE-3, F-AE-4, F-AE-5, F-AE-7 |

### Verification protocol per cell

For each list page: navigate to base URL, capture button state via
`document.querySelectorAll('button')` filter on Previous/Next, click
Next, capture URL + button state + row count, click Prev where
applicable, deep-link the page-2 cursor URL and confirm rows persist.

For tab tables: deep-link the namespaced cursor URL (`cursor_p`,
`cursor_t`, `cursor_e`, `cursor_i`, or default `cursor`) using
base64-encoded cursors obtained from `curl …?limit=N` probes.

### Cumulative tally re-stated

| Severity | Pre-delta | Post-delta |
|---|---:|---:|
| 🔴 | 3 | **2** |
| 🟠 | 30 | 30 |
| 🟡 | 48 | **49** (F-AE-6 demoted to 🟡 silent-shape-mismatch sub-finding) |
| 🟢 | 37 | 37 |

### Time spent

~30 min (faster than budgeted 45 min because all 5 list pages exercised
in batch + 3 tab tables sufficient to validate the `useCursorPagination`
infrastructure shared across all remaining sub-sections).

### Off-scope observation

`mock` fixture sizes are small for most endpoints (3–9 rows). Future
delta passes that need to fully exercise multi-page cursor flow on the
detail-sub-section endpoints would benefit from a fixture expansion task
(spawn `XXXX_CHORE_fixture-grow-for-pagination-tests` if Phase 3 wants
this). Not blocking F-D-1 resolution.


## Wave 5 — Tier 4 sequential

**Stage:** Tier 4 (heavy subjective; Claude drafts, user spot-checks)
**Model tier:** `claude-opus-4-7` extra-high (1M context) per task README "Model tier" switch instructions
**Baseline SHA:** `81928602` (post-0254 merge + Wave 4 close)

### Sub-phases run

| Sub-phase | Output file | Findings |
|---|---|---:|
| 1.2 spec/source consistency | `findings/A-AC-spec-source-consistency.md` | 7 |
| 1.3 file/folder structure | `findings/AH-file-folder-structure.md` | 8 (3 new + 5 cross-cite) |
| 1.9c Y simplicity | `findings/Y-simplicity.md` | 6 (1 new + 5 cross-cite) |
| 1.10 Z senior craft | `findings/Z-senior-craft.md` | 4 |
| 1.10b AA overengineering | `findings/AA-overengineering.md` | 6 |
| 1.10c AB hallucination | `findings/AB-hallucination.md` | 5 |
| 1.10d AD maintenance cost | `findings/AD-maintenance-cost.md` | 5 |

**Wave 5 total: 41 findings across 7 files** (many cross-cite previous waves — see per-file notes).

### Severity breakdown (Wave 5 only)

| Severity | Count |
|---|---:|
| 🔴 | 0 |
| 🟠 | 4 (F-AB-5 recap, F-Y-2 NEW, F-AD-1 recap, F-AD-5 recap) |
| 🟡 | 14 |
| 🟢 | 23 |

### Class breakdown (Wave 5 only)

| Class | Count |
|---|---:|
| A (baseline) | 8 (mostly positive/informational) |
| B (routing/contract) | 0 |
| C (visual/layout) | 12 |
| D (catalog) | 21 |
| E (off-band) | 0 |

### Cumulative tally — Waves 1-5 + post-merges

| Severity | Pre-Wave-5 | Wave 5 delta | Post-Wave-5 |
|---|---:|---:|---:|
| 🔴 | 2 | 0 | **2** |
| 🟠 | 30 | +1 NEW (F-Y-2) | **31** |
| 🟡 | 49 | +14 | **63** |
| 🟢 | 37 | +23 | **60** |
| **Total actionable** | ~118 | +38 unique (3 strict-new + 35 recap) | **~156** |

(Cross-cites count toward Wave 5 file inventory but do not double-count cumulative severity tally.)

### Top 5 highest-severity Wave 5 findings (NEW or first-classified-here)

1. **F-Y-2 [Class C, 🟠 NEW]** — debounce-and-re-sync pattern duplicated 4× across filter components (`TransactionFilters`, `AssetFilters`, `NftFilters`, `PoolsFilterBar`). Extract `useDebouncedDraft` hook.
2. **F-AD-1 [Class C, 🟠 recap of cross-cite]** — leaked-concern: 6-file change required to fix truncation; 4-file change for debounce; 3-file for stroop display. **Largest maintenance-cost smell in audit.**
3. **F-AB-5 [Class C, 🟠 recap]** — 6 distinct cross-task formatter/truncation/debounce duplications; consolidate Phase 3.
4. **F-AD-5 [Class D, 🟠 recap]** — zero test coverage; pre-launch test-baseline spawn priority.
5. **F-AH-3 [Class C, 🟡 NEW]** — `web/src/pages/detail/SectionCard.tsx` is shared chrome in wrong home; hoist to `libs/ui/src/layout/` consolidates F-U-1 + F-AH-3 into 1 PR.

### Subjective calls flagged for user spot-check (Tier 4 protocol)

1. **F-A-3 (1.2)** — partial doc-sync gap on 0254 (frontend-overview updated, backend-overview not). Severity could shift 🟡↔🟢 based on whether ADR amendment is considered sufficient.
2. **F-A-4 (1.2)** — LP feature as gold-standard exemplar is a subjective triangulation call.
3. **F-AH-2 (1.3)** — "asymmetric folder split is fine because the rule is consistent" — senior judgment; user may prefer enforced uniformity.
4. **F-AH-3 / F-AH-7 / F-AH-8 (1.3)** — multiple folder-structure cleanups; user decides priority/urgency.
5. **F-Y-1 (1.9c)** — `overrides.ts` 890-LOC split is judgment refactor.
6. **F-Y-2 (1.9c)** — debounce-extract: 4 sites = rule-of-3+ judgment; user decides.
7. **F-Y-4 (1.9c)** — `PoolCharts.tsx` 268 LOC borderline split.
8. **Z-1 Spot 5 / F-Z-2 (1.10)** — operation enum codegen needs backend coordination; pre- vs post-launch.
9. **Z-3 (1.10)** — 3-hop discoverability "acceptable" senior judgment.
10. **F-AA-1 (1.10b)** — `useDebounced` / `useIntersectionObserver` single-consumer trim toss-up.
11. **0061 #4 (1.10c)** — sort-caret middle ground requires designer sign-off (Wave 6).
12. **0251 B1 (1.10c)** — `linked={false}` fix-by-hide acceptability.
13. **F-AD-3 (1.10d)** — 3 magic-number polish (1500ms, 1062, 1064) — minor judgment.

### Phase 3 spawn candidates added by Wave 5

| Spawn task name | Source | Effort |
|---|---|---|
| `XXXX_DOCS_evergreen-doc-sync-0254-pagination` | F-A-3 | S |
| `XXXX_DOCS_frontend-conventions-wiki` (formatter homes, hook tiering, useDetailMode vs useTableUrlState) | F-AB-1, Z-3, F-AD-2 | S |
| `XXXX_REFACTOR_frontend-section-card-hoist` | F-AH-3 + F-U-1 + F-X-2 (consolidated) | S |
| `XXXX_REFACTOR_frontend-format-and-truncate-unification` | F-Y-6 + F-U-2/3/4 + F-J-3/16/17 + F-Y-2 (consolidated) | M |
| `XXXX_CHORE_frontend-pagestub-and-orphan-cleanup` | F-AH-1 | XS |
| `XXXX_REFACTOR_frontend-search-folder-restructure` | F-AH-7 | S |
| `XXXX_REFACTOR_frontend-pages-helpers-hoist` | F-AH-8 | XS |
| `XXXX_REFACTOR_frontend-overrides-split-per-family` | F-Y-1 | S |
| `XXXX_CHORE_frontend-magic-number-naming` | F-AD-3 | XS |
| `XXXX_DOCS_task-body-amend-useDetailMode-divergence` | F-AB-1 | XS |
| `XXXX_DOCS_task-body-amend-interval-labels-0065` | F-AB-2 | XS |
| `XXXX_FEATURE_frontend-pool-id-href-root-cause` | F-AB-3 | S |

Bundle these into 1-3 atomic batch tasks per Phase 3 sub-phase 3.3 (per 0251 model).

### Blockers hit

- **None.** All sub-phases ran sequentially without external dependency. No stack restart needed (pure grep + Read).
- `tools/dev-mock-api.mjs` referenced in Wave 5 plan AC-12 check **not present** in worktree — backend is the dev source-of-truth (verified). Adjusted AC-12 to N/A.

### Time spent

~6h equivalent (under 8h budget) — sub-phases benefited heavily from Wave 1-4 cross-citation, reducing fresh grep work. Tier 4 subjective writing dominated; tooling work was minimal.

### Readiness for Gate B + Wave 6

**Gate B readiness:** ✅
- All 7 Tier 4 sub-phases delivered; findings file inventory complete (24 finding files total in `findings/`).
- Class A/B/C findings identified for Gate B triage decisions.
- Zero new fix-first audit-blocker candidates (no Class B routing/contract findings emerged).
- Class C cluster: F-AH-3 (SectionCard hoist), F-Y-6 (formatter unification), F-AD-1 (leaked concerns) — all Phase 3 batch candidates per task README anti-fix-first anti-pattern ("Renamings / refactors without behavior change — Phase 3").

**Wave 6 readiness:** ⚠ pending Gate B triage decisions
- Track 2 (visual + UX): user review required for Figma fidelity 2.1 + Playwright re-pass 2.0 + responsive 2.4.
- Gate B can defer all Wave 5 Class C findings to Phase 3 since none change rendered DOM in ways that would invalidate visual baseline (SectionCard hoist preserves visual API — only home changes).
- Recommendation: **proceed to Gate B triage** with note that Wave 5 surfaces no new fix-first scope; all Class C consolidation is deferable to Phase 3 batch.

### Worklog completeness

Wave 5 worklog ready for handoff to Gate B session. Continue session in extra-high (1M context) per task README "stay on extra-high" directive for Gate B + Phase 3 consolidation.


## Gate B — triage decisions written

**Time:** end of Wave 5, before Wave 6.
**Output:** `findings/triage-gate-B.md` (read-only decision doc).

**Result summary:**
- 11 Class A baseline-breakers → 0 fix-first / 11 defer Phase 3 (type-safety flags + bundle perf + folder structure — all bundle into Phase 3 batch tasks)
- 10 Class B routing/contract → **6 fix-first** (3 audit-blocker tasks consolidating: F-D-2/F-AE-5 composite NotFound; F-K-2/F-K-3 pool detail link gaps; F-L-1/F-K-4 strkey search canonicalisation) / 4 defer Phase 3
- ~20 Class C visual/layout → **2 fix-first** (CA-1/CA-2 Footer dead links — pre-launch legal must-fix bundled into one task) / ~18 defer Phase 3
- ~85 Class D → all defer Phase 3 bulk-spawn
- 2 Class E off-band → immediate (Vite 7.3.3 CVE bump task + CORS infra comms question)

**Gate B fix-first scope: 4 audit-blocker tasks**:
1. `XXXX_BUG_composite-notfound-sub-section-queries.md` (F-D-2 + F-AE-5)
2. `XXXX_BUG_pool-detail-missing-asset-and-ledger-links.md` (F-K-2 + F-K-3)
3. `XXXX_BUG_search-strkey-canonical.md` (F-L-1 + F-K-4)
4. `XXXX_PRELAUNCH_footer-legal-and-external-links.md` (CA-1 + CA-2) — requires user decision (real hrefs vs hide)

**Plus off-band:**
- 5. `XXXX_CHORE_vite-7.3.3-cve-bump.md` (single-line dep bump)
- 6. CORS comms question to backend/infra owner (no task spawn)

**Cascade compression estimate:** ~30 duplicate findings avoided in Wave 6
Track 2 output by landing these 4 fix-firsts.

**Awaiting user signal** before spawning audit-blocker backlog tasks.


## Exhaustive sweep 2026-05-26 (pre-Wave-6)

**Trigger:** user flagged that several findings cited "samples" not exhaustive
counts. Pre-Wave-6 (Track 2 visual + UX) needs clean baseline so Track 2
measures actual residual issues, not previously-undetected siblings.

**Mode:** read-only audit; no production code edits; findings file appends OK;
task 0262 (composite NotFound) report-only — user extends.

**Sweep scope:** 6 clusters.

### Per-cluster deltas

| Cluster | Original | Exhaustive | Delta |
|---|---|---|---|
| 1 Composite NotFound | F-D-2 cited account+contract (E6+E8+E9) | **E6 account + E9 contract + E13 pool** (E8 asset is CLEAN — render-gate present) | Swap E8 → E13; affected count still 3 |
| 2 Cross-entity links | F-K-2 reserves cited 1 site (PoolSummary) | **3 sites:** PoolSummary + PoolKpiStrip + PoolsTable list page | +2 surfaces |
| 3 Truncation re-impls | F-U-3 cited 6 ad-hoc impls | **6 confirmed** | 0 — exhaustive |
| 4a STROOPS_PER_XLM | F-U-4 cited 2 sites | **2 confirmed** | 0 — exhaustive |
| 4b toLocaleString | J-2 cited 7 sites | **10 sites** | +3 |
| 4c toFixed bypass | J-3 cited 3 sites | **4 sites** | +1 |
| 5 Type-escape casts | F-AQ-7 cited "3 files in advanced/" | **8 files across tx-detail + 3 API + 2 libs/ui** | broader surface; pattern unchanged |
| 6 URL-state consumers | AL Part 2 KEEP verdict | confirmed 13× useCursorPagination + 1× useTabUrlState + 1× useDetailMode + 1× raw useSearchParams; **+1 candidate F-EX-2** (PoolCharts metric/period useState) | +1 |

### New findings spawned

- **F-EX-1** [Class C, 🟡 MEDIUM] — NFT detail `minted_at_ledger` plain text per Figma; tension with every-other-ledger-linked pattern. Defer Gate B Figma cross-reference.
- **F-EX-2** [Class C, 🟢 LOW] — Pool chart metric/period in useState not URL; deep-link enhancement. Defer Gate B.

**Severity escalations: 0.** All cluster patterns within ±1-3 of original
counts, no severity tier changes warranted.

### Findings files updated (appended sections, no rewrites)

- `findings/D-state-coverage-matrix.md` — F-D-2 scope correction (E8 clean, E13 newly affected)
- `findings/K-cross-entity-links.md` — F-K-2 extends to PoolKpiStrip + PoolsTable; F-EX-1 captured
- `findings/J-data-formatting.md` — J-2 / J-3 exhaustive counts
- `findings/U-component-reuse.md` — F-U-3 / F-U-4 confirmed exhaustive
- `findings/AQ-type-safety.md` — F-AQ-7 surface area broadened, no escalation
- `findings/AL-state-separation.md` — F-EX-2 captured
- `findings/exhaustive-sweep-2026-05-26.md` — full summary file (new)

### Task-scope implications

- **0262 composite NotFound (user-owned extension):** swap E8 asset → E13 pool;
  affected pages = `AccountDetailPage.tsx`, `ContractDetailPage.tsx`,
  `LiquidityPoolDetailPage.tsx`. E8 already implements correct render-gate
  pattern — reference implementation at `AssetDetailPage.tsx:127`.
- **0263 pool detail links + backend schema:** scope expands from 1 site to
  3 sites — `PoolSummary` + `PoolKpiStrip` + `PoolsTable`. All share root
  cause F-K-9 (`PoolAssetLeg` schema gap). User confirms task 0263 PR
  covers all 3.
- **0264 strkey canonical:** no additional surfaces; pool is sole case.
- **0265 Vite CVE:** out of scope; no implication.

### Recommendation

**Wave 6 readiness: GREEN.**

- 0 new HIGH/CRITICAL findings
- 2 new MEDIUM/LOW findings (both Class C, defer Gate B)
- Cluster 1 scope correction is the most actionable outcome
- Baseline now exhaustive across 6 clusters — Wave 6 Track 2 can measure
  residual visual / Figma drift against known-exhaustive code-level
  baseline

**Blockers hit:** none. All greps + reads completed cleanly.

Awaiting user signal to proceed with Wave 6 (Track 2 visual + UX audit).
