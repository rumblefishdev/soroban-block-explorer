# C — API consistency cross-checks (1.4)

**Wave:** 2
**Stance:** senior fresh-eye, read-only
**Date:** 2026-05-25
**Operator:** karolkow (single Claude agent, sequential)

Cross-checks driven by 0251 history (H2 lowercase op param, H6 5-vs-27 ops,
H3 polymorphic asset URLs, B2 NUMERIC stringification trim, B4 issuer disambig).

## Summary table

| #    | Check                                                        | Verdict          | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Severity  | Class                                                             |
| ---- | ------------------------------------------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------- |
| C-1  | Case sensitivity tolerance for `op` URL param                | ✓ ok             | `web/src/pages/transactions/operationTypes.ts:71` `normalizeOperationType` trims + uppercases + allow-lists, returns `''` if unknown — fixes H2 root cause                                                                                                                                                                                                                                                                                                                                                          | —         | —                                                                 |
| C-2  | Operation-type enum parity FE↔BE (27 variants)               | ✓ ok             | FE `web/src/pages/transactions/operationTypes.ts:22` 27 entries; BE `crates/domain/src/enums/operation_type.rs:21` 27 entries; member-by-member identical, declaration order matches XDR discriminant                                                                                                                                                                                                                                                                                                               | —         | —                                                                 |
| C-3  | Other enums (assetType, networkStatus, polling) parity FE↔BE | partial          | no per-enum FE catalog file outside operationTypes; assetType strings consumed directly from generated `@rumblefish/api-types`, so structural sync via OpenAPI rather than mirrored TS enum — acceptable but undocumented as a convention                                                                                                                                                                                                                                                                           | 🟢 LOW    | D                                                                 |
| C-4  | Polymorphic IDs — link builders normalize/strip              | partial          | `web/src/router/routes.ts:13,18,21` apply `encodeURIComponent` to `asset`, `nft`, `pool`; `transaction`, `ledger`, `account`, `contract` pass through unencoded (raw G…/C…/hex are URL-safe by alphabet so OK in practice) — but inconsistency is undocumented                                                                                                                                                                                                                                                      | 🟢 LOW    | D                                                                 |
| C-5  | Asset polymorphic ID strategy clear                          | gap              | no `isAssetId` / `isNftId` validator in `libs/ui/src/identifiers/validators.ts`; `isValidIdentifier` falls back to `value.length > 0` for `asset` + `nft` (line 53). BE accepts multiple shapes per H3 archive but FE has no shape-aware probing → invalid asset id reaches API instead of failing client-side                                                                                                                                                                                                      | 🟡 MEDIUM | B                                                                 |
| C-6  | Pool id strkey vs hex round-trip                             | ✓ ok             | `web/src/utils/poolIdStrkey.ts` is single hex→strkey boundary; `isPoolId` `libs/ui/src/identifiers/validators.ts:35` validates 64-char hex (tolerates upper-case via `.toLowerCase()` — resilient deep-linking)                                                                                                                                                                                                                                                                                                     | —         | —                                                                 |
| C-7  | Date/timezone semantics UTC consistent                       | partial          | `web/src/pages/transactions/formatters.ts:22` `formatAbsoluteUtc` uses `getUTC*` and appends `UTC`; `libs/ui/src/timestamps/RelativeTimestamp.tsx:30` puts ISO in tooltip; `web/src/api/hooks/usePoolChart.ts:26-40` `toISOString()` for from/to. **All UTC.** ✓                                                                                                                                                                                                                                                    | —         | —                                                                 |
| C-8  | `<time dateTime>` semantic element                           | gap              | zero hits for `<time dateTime` or `<time>` across `web/src/` + `libs/ui/src/`. Timestamps render in `Typography` only — degrades SR + machine-readability; tooltip ISO is hover-only                                                                                                                                                                                                                                                                                                                                | 🟢 LOW    | D                                                                 |
| C-9  | Number precision — trailing zero trim                        | ✓ ok             | `web/src/pages/format.ts:21` `frac.replace(/0+$/, '')`; `1.500000` → `1.5`; `web/src/pages/transactions/formatters.ts:14` `formatFee` `replace(/\.?0+$/, '')`; consistent                                                                                                                                                                                                                                                                                                                                           | —         | —                                                                 |
| C-10 | Number precision — `minDecimals` floor                       | ✓ ok             | `formatAmount(value, 2)` pads up to 2 (line 22), used by `AccountBalances`, `PoolParticipants`, fee % — consistent                                                                                                                                                                                                                                                                                                                                                                                                  | —         | —                                                                 |
| C-11 | Null vs missing — em-dash convention                         | partial          | `formatAmount` returns `'—'` for null/invalid (`web/src/pages/format.ts:16,18`), `Dash()` cell renders `'—'` (`web/src/pages/transactions/cells.tsx:7`), `RelativeTimestamp` `FALLBACK = '—'`; `?? '—'` patterns ok in 3 hit sites. **But:** `web/src/pages/AccountDetailPage.tsx:23` + `web/src/pages/contracts/ContractEvents.tsx:47` use `…` (horizontal ellipsis U+2026) as truncation glue — different glyph, different semantics (truncation vs missing) — both legitimate but no project-wide convention doc | 🟢 LOW    | D                                                                 |
| C-12 | Em-dash vs hyphen inconsistency                              | ✓ ok             | zero hits for `?? '-'` (ASCII hyphen) as null placeholder; em-dash exclusive                                                                                                                                                                                                                                                                                                                                                                                                                                        | —         | —                                                                 |
| C-13 | Cursor pagination — per-endpoint semantic identical          | ✓ ok             | every list hook (`useTransactionsList`, `useLedgersList`, `usePoolsList`, `useNftsList`, `useAccountTransactions`, `useAssetTransactions`, `useContractEvents`, `usePoolParticipants`, `usePoolTransactions`, `useNftTransfers`) passes `{ cursor }` query when truthy; cursor is part of queryKey (cache-per-cursor); shape uniform                                                                                                                                                                                | —         | —                                                                 |
| C-14 | `useInfinitePager` / `useCursorPagination` single hook       | ✓ ok             | name referenced in hook docstrings as `useCursorPagination`; URL-as-state pattern; consistent                                                                                                                                                                                                                                                                                                                                                                                                                       | —         | —                                                                 |
| C-15 | Polling cache headers — interval defined per endpoint        | partial          | `web/src/api/polling.ts` defines 4 policies: `homePolicy` (10s stale / 12s refetch), `listPolicy` (60s stale, keepPreviousData), `detailPolicy` (5min stale), `searchPolicy` (0/0). BE serves `Cache-Control` (see `crates/api/src/common/cache_control.rs`, tested at `tests_integration.rs:1953/2134/2192/3434` — values: `public, max-age=10` for head, `300` for closed, `60`, `no-store` for validation 400). FE `staleTime` not aligned to BE max-age values — minor smell, not a bug                         | 🟢 LOW    | D                                                                 |
| C-16 | Polling pause on tab inactive                                | not-checked-here | `visibilitychange` check deferred to sub-phase 1.22 (next wave)                                                                                                                                                                                                                                                                                                                                                                                                                                                     | —         | —                                                                 |
| C-17 | CORS production-configured (API side)                        | 🔴 absent        | `grep tower_http::cors\|CorsLayer` across `crates/api/src/` returns zero hits in production code paths; only `tower::ServiceExt` used (test harness + cache_control). **No `CorsLayer` registered on Router.** Production deploy will reject browser requests from any non-same-origin frontend. CloudFront/API Gateway may layer it on but that's invisible from repo                                                                                                                                              | 🟠 HIGH   | E (off-band — infra/deploy, not FE bug per se, but blocks launch) |
| C-18 | FE client `credentials` setting                              | ✓ ok-by-default  | `web/src/api/client.ts:1-5` only sets `baseUrl`; uses generated `client` from `@rumblefish/api-types` with default `credentials: 'same-origin'`; no cookies sent cross-origin. **However:** if API runs on different origin in prod, with CORS configured to allow credentials, FE would not send any — acceptable for read-only public explorer                                                                                                                                                                    | —         | —                                                                 |
| C-19 | Error envelope shape post-interceptor                        | ✓ ok             | `web/src/api/client.ts:11-29` always returns a real `Error` instance with `.status` + `.body` preserving the ADR 0008 envelope — consumers get `.message` + status code; consistent across all hooks                                                                                                                                                                                                                                                                                                                | —         | —                                                                 |
| C-20 | API base URL configurable per env                            | ✓ ok             | `web/src/api/config.ts:1-15` reads `VITE_API_BASE_URL`, throws on missing or invalid URL, strips trailing slash — single source of truth                                                                                                                                                                                                                                                                                                                                                                            | —         | —                                                                 |

## Cross-references to Wave 1

- **C-1** confirms H2 (0251 fixed) — `normalizeOperationType` is the canonical guard. ✓
- **C-2** confirms H6 (0251 fixed) — 27 ops parity holds. ✓
- **C-5** echoes H3 polymorphic asset URL gap; not regressed by 0251 but never fully addressed in FE validator layer — flag for Phase 3.
- **C-9, C-10** confirm B2 trim trailing zeros works in `formatAmount`. ✓
- **C-15** echoes Wave 1 F-AF-\* — generated client used everywhere; polling layer is a thin wrapper, no manual fetches.

## Top issues

1. **C-17 (🟠 HIGH, Class E):** no `CorsLayer` registered in `crates/api/src/`. Off-band — likely covered by infra (API Gateway / ALB) but not visible from repo. Document or fail.
2. **C-5 (🟡 MEDIUM, Class B):** `isValidIdentifier` for `asset` + `nft` is "any non-empty string" — invalid IDs reach API. Strengthen with shape-aware validators.
3. **C-3 (🟢 LOW, Class D):** non-operation enums have no FE mirror; OpenAPI is the only contract. Conventionally fine, but undocumented.
4. **C-8 (🟢 LOW, Class D):** no `<time dateTime>` element — a11y/machine-readable timestamps degraded.
5. **C-11 (🟢 LOW, Class D):** em-dash vs ellipsis convention undocumented (both used legitimately).

## Notes

- Polling pause-on-inactive + endpoint→policy mapping survey deferred to 1.22 (Wave 3).
- CORS check repeated in 1.19 AO build-deploy.
