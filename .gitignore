# Company Cloudflare/DNS scaffold — belongs in the private `rf-domains` repo,
# never in this public repo (task 0277 D9). Generated locally; move it out.
rf-domains/

# dependencies
node_modules

# build output
dist
tmp
out-tsc
coverage
*.tsbuildinfo

# nx
.nx/installation
.nx/cache
.nx/workspace-data
.nx/polygraph

# environment
.env
.env.local
.env.*.local
# per-dev local config (may hold a dev x-api-key for Option-B proxy) — never commit
.env.development

# editors
/.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.claude/worktrees
.claude/settings.local.json
.claude/settings.local.json.*
.claude/scheduled_tasks.lock

# logs
npm-debug.log*
yarn-error.log
testem.log

# system files
.DS_Store
Thumbs.db

# generated timestamps
vite.config.*.timestamp*
vitest.config.*.timestamp*

# graphify knowledge-graph analysis output (generated by /graphify; not source)
graphify-out/

# Lore framework - session-local files
lore/0-session/current-user.md
lore/0-session/current-task.md
lore/0-session/current-task.json
lore/0-session/next-tasks.md
lore/README.md
lore/BOARD.md
lore/board.json

.npmrc


.temp
.artifacts
.trash

# CDK
cdk.out
# Context cache contains AWS account ID — each deployer generates their own
cdk.context.json

# Rust build artifacts
target/

# Local Docker compose overrides (perf tunings, env-specific paths)
docker-compose.override.yml
docker-compose.override.yaml

# Playwright MCP runtime artefacts (snapshots + console logs from
# browser-driven manual verification sessions)
.playwright-mcp/
# local deletion graveyard (never committed)
.trash/

# Claude Code local dev-server wiring — machine-specific, pairs with the
# gitignored web/.env.development.local. Shared .claude/skills/ stays tracked.
.claude/launch.json
