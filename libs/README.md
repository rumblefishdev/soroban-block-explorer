# Libs

Shared TypeScript libraries for the frontend workspace.

## libs/ui

Shared React component library (Vite lib mode). Contains reusable
presentation primitives and frontend-only view types used by `web/`.

## Note

Backend domain types, database access, and error handling live in the
Rust workspace (`crates/`). See ADR 0005 for the Rust-only backend decision.
