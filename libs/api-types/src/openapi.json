{
  "openapi": "3.1.0",
  "info": {
    "title": "Soroban Block Explorer API",
    "description": "REST API exposing ledger, transaction, contract, and NFT data for the Soroban block explorer. All list endpoints use cursor-based pagination (see ADR 0008).",
    "contact": {
      "name": "Rumble Fish",
      "url": "https://rumblefish.dev"
    },
    "license": {
      "name": "Proprietary"
    },
    "version": "0.1.0"
  },
  "paths": {
    "/health": {
      "get": {
        "tags": ["ops"],
        "summary": "Liveness probe consumed by AWS Lambda health checks and smoke tests.",
        "operationId": "health",
        "responses": {
          "200": {
            "description": "Service is healthy",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          }
        },
        "security": [{}]
      }
    },
    "/v1/accounts": {
      "get": {
        "tags": ["accounts"],
        "summary": "List accounts ordered by `last_seen_ledger` (the only indexed sort) —\nnewest-active first by default, oldest-first with `?order=asc`. The order\nis sticky across pages; cursor pagination walks within it.\n`filter[with_domain]` keeps only accounts that set a home_domain. No\naddress search — exact lookup is the global search's redirect path. Same\nshape as the other list endpoints.",
        "operationId": "list_accounts",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "order",
            "in": "query",
            "description": "Base sort order on `last_seen_ledger`: `asc` | `desc` (default).\nSticky — re-send on every page alongside `cursor`.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[with_domain]",
            "in": "query",
            "required": false,
            "schema": {
              "type": ["boolean", "null"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated account list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_AccountListItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/accounts/{account_id}": {
      "get": {
        "tags": ["accounts"],
        "summary": "Account detail — header from `accounts` + balances from\n`account_balances_current` (canonical 06 statements A + B).",
        "operationId": "get_account",
        "parameters": [
          {
            "name": "account_id",
            "in": "path",
            "description": "Stellar account StrKey (G…, 56 chars)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Account detail with current balances",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AccountDetailResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid account_id",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Account not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/accounts/{account_id}/transactions": {
      "get": {
        "tags": ["accounts"],
        "summary": "Paginated transactions involving the account (source or participant).\n404 when the StrKey is unknown — distinct from \"indexed account, no\ntransactions yet\" (matches assets/contracts sub-resource pattern).",
        "operationId": "list_account_transactions",
        "parameters": [
          {
            "name": "account_id",
            "in": "path",
            "description": "Stellar account StrKey (G…, 56 chars)",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "order",
            "in": "query",
            "description": "Sort order on transaction time: `asc` | `desc` (default).\nSticky — re-send on every page alongside `cursor`.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated transactions involving the account",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_AccountTransactionItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid account_id / pagination",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Account not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/assets": {
      "get": {
        "tags": ["assets"],
        "operationId": "list_assets",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "filter[type]",
            "in": "query",
            "description": "`native | classic_credit | soroban` (ADR 0051 — `sac` is no longer a\ntype; use `filter[sac]` for the SAC view).",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[code]",
            "in": "query",
            "description": "Substring match against `asset_code`; SQL wraps in `%...%`.\nCaller MUST NOT pass `%` / `_` literals.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[sac]",
            "in": "query",
            "description": "SAC property filter (ADR 0051): `true` restricts the list to assets whose\nStellar Asset Contract is deployed on-chain (`sac_deployed`) — the old\n`filter[type]=sac` view, now a facet predicate over classic_credit /\nnative rows. Reserved (un-deployed) SAC addresses are excluded. Any other\nvalue is ignored.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated asset list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_AssetItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/assets/{id}": {
      "get": {
        "tags": ["assets"],
        "operationId": "get_asset",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Contract StrKey (C…, 56 chars), `CODE-ISSUER` composite (e.g. USDC-GA…), or the reserved `native` token for XLM.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Asset detail",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AssetDetailResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid id format",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Asset not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/assets/{id}/transactions": {
      "get": {
        "tags": ["assets"],
        "operationId": "list_asset_transactions",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Contract StrKey (C…, 56 chars), `CODE-ISSUER` composite (e.g. USDC-GA…), or the reserved `native` token for XLM.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated transactions involving the asset",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_AssetTransactionItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid id format / pagination",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Asset not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/contracts": {
      "get": {
        "tags": ["contracts"],
        "summary": "List contracts, newest-ingested first (`id DESC`, the PK order — no\nuser sort). `filter[type]` narrows by class, `filter[q]` searches\nid/name. Cursor-paginated like every other list endpoint.",
        "operationId": "list_contracts",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "filter[type]",
            "in": "query",
            "description": "Contract class: `token | other | nft | fungible`.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[q]",
            "in": "query",
            "description": "Free-text search over contract id + name (`search_vector`).",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated contract list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_ContractListItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/contracts/{contract_id}": {
      "get": {
        "tags": ["contracts"],
        "operationId": "get_contract",
        "parameters": [
          {
            "name": "contract_id",
            "in": "path",
            "description": "Contract StrKey (C…, 56 chars)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Contract detail",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ContractDetailResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid contract_id",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Contract not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/contracts/{contract_id}/decompiled": {
      "get": {
        "tags": ["contracts"],
        "summary": "Decompile the contract's WASM on demand (task 0465, refs #374).",
        "description": "No persistence: bytes are fetched from Soroban RPC and decompiled per\nrequest. The output is immutable per (`wasm_hash`, decompiler version),\nwhich justifies the `LONG` cache header even without a server-side cache.",
        "operationId": "get_decompiled",
        "parameters": [
          {
            "name": "contract_id",
            "in": "path",
            "description": "Contract StrKey (C…, 56 chars)",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "format",
            "in": "query",
            "description": "Requested representation: `rust` (default) or `wat`. When `rust` is\nrequested but emission fails, the response carries the WAT fallback\nwith `representation: \"wat\"` and `rust_error` set — no second\nround-trip needed.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Decompiled source (Rust, or WAT fallback)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DecompiledResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid contract_id or format",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Contract not found, has no WASM (SAC / pre-upload), or code no longer live",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "WASM fetch or decompilation failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/contracts/{contract_id}/events": {
      "get": {
        "tags": ["contracts"],
        "operationId": "list_events",
        "parameters": [
          {
            "name": "contract_id",
            "in": "path",
            "description": "Contract StrKey (C…, 56 chars)",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20). The page is per event\n(one row → one item), so `data.len() <= limit`.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated event history",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_EventItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid contract_id / limit / cursor",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Contract not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/contracts/{contract_id}/interface": {
      "get": {
        "tags": ["contracts"],
        "operationId": "get_interface",
        "parameters": [
          {
            "name": "contract_id",
            "in": "path",
            "description": "Contract StrKey (C…, 56 chars)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Public function signatures",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InterfaceResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid contract_id",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Contract not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/contracts/{contract_id}/invocations": {
      "get": {
        "tags": ["contracts"],
        "operationId": "list_invocations",
        "parameters": [
          {
            "name": "contract_id",
            "in": "path",
            "description": "Contract StrKey (C…, 56 chars)",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20). One DB appearance row maps to\none `InvocationItem` (no expansion), so `data.len() <= limit`.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated invocation appearance index",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_InvocationItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid contract_id / limit / cursor",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Contract not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/ledgers": {
      "get": {
        "tags": ["ledgers"],
        "summary": "List ledgers ordered by `(closed_at, sequence)` — newest-first by\ndefault, oldest-first with `?order=asc`. The order is sticky across\npages; cursor pagination walks forward/back within the chosen order.",
        "operationId": "list_ledgers",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "order",
            "in": "query",
            "description": "Base sort order: `asc` = oldest→newest, `desc` (default) = newest→oldest. Sticky — re-send it on every page alongside `cursor`. Reset to the first page (drop `cursor`) when changing it.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated ledger list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_LedgerListItem"
                }
              }
            }
          },
          "304": {
            "description": "Not Modified — `If-None-Match` matched the current chain head (live first page only)"
          },
          "400": {
            "description": "Invalid query parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/ledgers/{sequence}": {
      "get": {
        "tags": ["ledgers"],
        "summary": "Get ledger detail by sequence — header + prev/next navigation +\nembedded paginated transactions.",
        "description": "Two phases, both DB-only:\n\n1. **DB header.** Resolve `:sequence` against `ledgers` + LATERAL\n   prev/next computed via `sequence` comparisons on the `ledgers`\n   PK (index-only scan, no heap fetch). 404 on miss.\n2. **DB transactions.** Keyset-paginated read of the `transactions`\n   partition with full equality partition prune\n   (`created_at = $closed_at`).\n\nThe detail endpoint reuses the standard `?limit=` / `?cursor=` query\nparameters to drive embedded transactions pagination. Detail itself\nis a single resource, so no naming collision arises — the params\npage the embedded list directly.",
        "operationId": "get_ledger",
        "parameters": [
          {
            "name": "sequence",
            "in": "path",
            "description": "Ledger sequence number",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int64"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Embedded transactions page size (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Embedded transactions opaque pagination cursor.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Ledger detail with embedded transactions",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LedgerDetailResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid sequence format or pagination parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Ledger not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/liquidity-pools": {
      "get": {
        "tags": ["liquidity-pools"],
        "operationId": "list_pools",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "filter[asset_code]",
            "in": "query",
            "description": "Free-text asset filter — case-insensitive substring of either\n`asset_a_code` or `asset_b_code` (input is trimmed before the\nquery). The needle is matched literally: `%`, `_` and regex\nmetacharacters have no special meaning.\n\nA `/` makes it a **pair** query: `USDC/XLM` requires both codes to be\npresent, one on each leg, and the typed order does not matter. Only the\nfirst `/` splits, so `USDC/XLM/BTC` searches for the literal second code\n`XLM/BTC` and therefore matches nothing — a pool has two legs.\n\nNative legs match on `XLM` even though they store an empty code, so\n`XLM` returns the pools that actually hold native XLM. Note that it\n*also* returns credit assets minted under the code `XLM` — asset codes\nare not unique on Stellar, and this filter matches codes, not asset\nidentity. Callers needing one specific issuer's asset should use the\nper-leg `filter[asset_a_code]` + `filter[asset_a_issuer]` pair.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[asset_a_code]",
            "in": "query",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[asset_a_issuer]",
            "in": "query",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[asset_b_code]",
            "in": "query",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[asset_b_issuer]",
            "in": "query",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[min_tvl]",
            "in": "query",
            "description": "Minimum TVL threshold as a decimal string (matches the underlying\n`NUMERIC(28,7)` column without an f64 round-trip).",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated liquidity-pool list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_PoolItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/liquidity-pools/{pool_id}": {
      "get": {
        "tags": ["liquidity-pools"],
        "operationId": "get_pool",
        "parameters": [
          {
            "name": "pool_id",
            "in": "path",
            "description": "Pool ID — SEP-23 strkey (`L...`, 56 chars). Internal DB form is hex (ADR 0024); strkey is the canonical wire form.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Pool detail",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PoolItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid pool_id",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Pool not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/liquidity-pools/{pool_id}/chart": {
      "get": {
        "tags": ["liquidity-pools"],
        "operationId": "get_pool_chart",
        "parameters": [
          {
            "name": "pool_id",
            "in": "path",
            "description": "Pool ID — SEP-23 strkey (`L...`, 56 chars).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "interval",
            "in": "query",
            "description": "Bucket width: `1h` | `1d` | `1w`. Validated against an allowlist.\nDefault: `1d`.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "from",
            "in": "query",
            "description": "Inclusive lower bound, ISO 8601 / RFC 3339 timestamp.\nDefault: `to` minus the interval-appropriate window (see struct doc).",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "to",
            "in": "query",
            "description": "Exclusive upper bound, ISO 8601 / RFC 3339 timestamp.\nDefault: `now()`.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Time-bucketed pool chart series",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChartResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid pool_id / interval / from / to",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Pool not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Database error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/liquidity-pools/{pool_id}/participants": {
      "get": {
        "tags": ["liquidity-pools"],
        "operationId": "list_participants",
        "parameters": [
          {
            "name": "pool_id",
            "in": "path",
            "description": "Pool ID — SEP-23 strkey (`L...`, 56 chars). Internal DB form is hex (ADR 0024); strkey is the canonical wire form.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated participants list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_ParticipantItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid pool_id, limit, or cursor",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Pool not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Database error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/liquidity-pools/{pool_id}/transactions": {
      "get": {
        "tags": ["liquidity-pools"],
        "operationId": "list_pool_transactions",
        "parameters": [
          {
            "name": "pool_id",
            "in": "path",
            "description": "Pool ID — SEP-23 strkey (`L...`, 56 chars).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated pool transactions",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_PoolTransactionItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid pool_id, limit, or cursor",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Pool not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Database error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/network/stats": {
      "get": {
        "tags": ["network"],
        "summary": "Get top-level chain overview stats.",
        "description": "Reads the canonical single-statement network-stats query (latest\nledger row + `ledgers` 60s aggregate for TPS + planner row-count\nestimates for accounts / contracts) and caches the assembled\nresponse **keyed on the chain head** (`latest_ledger_sequence`) in\nprocess memory — see `network/cache.rs`. See the task 0045 spec and\n`docs/architecture/database-schema/endpoint-queries-clickhouse/01_get_network_stats.sql`\nfor the full data-source mapping.\n\nPer request we first read the head cheaply (`crate::common::head` —\n`SELECT max(sequence)`, a primary-key probe) and look up the cache\nunder it: an unchanged head is a HIT (the new ledger has not landed),\nan advanced head is a MISS that recomputes once. So a new ledger is\nvisible on the **first** request after it is written — there is no\nup-to-TTL window serving the previous head (task 0291).\n\nConcurrent cold-cache requests deduplicate via\n`moka::future::Cache::try_get_with` — the first task runs the\nasync DB query and the rest wait on its result instead of fanning\nout N round-trips.",
        "operationId": "get_network_stats",
        "responses": {
          "200": {
            "description": "Chain overview stats",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NetworkStats"
                }
              }
            }
          },
          "304": {
            "description": "Not Modified — `If-None-Match` matched the current chain head"
          },
          "500": {
            "description": "Database error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/nfts": {
      "get": {
        "tags": ["nfts"],
        "operationId": "list_nfts",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "filter[collection]",
            "in": "query",
            "description": "Exact match against `nfts.collection_name` (btree\n`idx_nfts_collection`). Trigram support is task 0132.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[contract_id]",
            "in": "query",
            "description": "Contract C-StrKey; resolved to `soroban_contracts.id` server-side.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[name]",
            "in": "query",
            "description": "Substring match against `nfts.name` via the `idx_nfts_name_trgm`\nGIN index. SQL wraps the value in `%...%`; caller MUST NOT pass\n`%` / `_` literals.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated NFT list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_NftItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/nfts/{contract_id}/{token_id}": {
      "get": {
        "tags": ["nfts"],
        "operationId": "get_nft",
        "parameters": [
          {
            "name": "contract_id",
            "in": "path",
            "description": "Contract C-StrKey hosting the NFT (CAP-46-6 token contract).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "token_id",
            "in": "path",
            "description": "Contract-defined token id (opaque string; ≤128 ASCII chars).",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "NFT detail",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NftDetailResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid contract_id / token_id",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "NFT not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/nfts/{contract_id}/{token_id}/transfers": {
      "get": {
        "tags": ["nfts"],
        "operationId": "list_nft_transfers",
        "parameters": [
          {
            "name": "contract_id",
            "in": "path",
            "description": "Contract C-StrKey hosting the NFT.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "token_id",
            "in": "path",
            "description": "Contract-defined token id (opaque string; ≤128 ASCII chars).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated NFT transfer history",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_NftTransferItem"
                }
              }
            }
          },
          "400": {
            "description": "Invalid contract_id / token_id / pagination",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "NFT not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/search": {
      "get": {
        "tags": ["search"],
        "summary": "Unified search across all entity types.",
        "description": "`?q=` is required. `?type=` (CSV) restricts the result to specific\nentity types — values must be in the closed allowlist\n(`transaction`, `contract`, `asset`, `account`, `nft`, `pool`).\n`?limit=` caps each entity bucket independently (default 10,\nceiling 50).\n\nBehaviour (task 0271):\n* One SQL path: broad search across the six entity-typed CTEs.\n* Response is `{ \"groups\": {…} }` with up to `limit` rows per\n  entity bucket. Rows carry the same columns regardless of\n  bucket: `entity_type`, `identifier`, `label`, `route_token`\n  (asset routing token or `null`), plus optional enrichment\n  (`successful`, `last_activity_at`) and composite routing\n  (`contract_id`, `token_id`) fields.\n* FE decides \"singleton → direct navigation\" by inspecting the\n  response: total row count == 1 and `routeForHit(singleton)`\n  resolves ⇒ navigate; else show the dropdown / list.\n\nAuthoritative SQL:\n`docs/architecture/database-schema/endpoint-queries-clickhouse/22_get_search.sql`.",
        "operationId": "get_search",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "description": "Search query string. Required, non-empty after trim.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "type",
            "in": "query",
            "description": "CSV of entity types to include. Allowed: transaction, contract, asset, account, nft, pool.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Per-group result cap. Default 10, max 50.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 50,
              "minimum": 1
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Search results",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SearchResults"
                }
              }
            }
          },
          "400": {
            "description": "Validation error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Database error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/transactions": {
      "get": {
        "tags": ["transactions"],
        "summary": "List transactions with optional filters and cursor-based pagination.",
        "operationId": "list_transactions",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Items per page (1–100, default 20).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "maximum": 100,
              "minimum": 1
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque pagination cursor from a previous response.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "filter[source_account]",
            "in": "query",
            "description": "Filter by source account StrKey (G…).",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[contract_id]",
            "in": "query",
            "description": "Filter by contract StrKey (C…) — matches root op, nested call, or event emission.",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          },
          {
            "name": "filter[operation_type]",
            "in": "query",
            "description": "Filter by operation type (e.g. `INVOKE_HOST_FUNCTION`).",
            "required": false,
            "schema": {
              "type": ["string", "null"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated transaction list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_TransactionListItem"
                }
              }
            }
          },
          "304": {
            "description": "Not Modified — `If-None-Match` matched the current chain head (live first page only)"
          },
          "400": {
            "description": "Invalid query parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/transactions/{hash}": {
      "get": {
        "tags": ["transactions"],
        "summary": "Get a single transaction by hash.",
        "description": "Returns the wrapped E3 response from task 0150: the DB-sourced\n`TransactionDetailLight` (flattened to the top level) plus a `heavy` block\ncarrying every XDR-sourced field — memo, result_code, signatures, fee-bump\nsource, envelope/result XDR, contract + diagnostic events, per-operation\ndecoded details, and the nested `operation_tree`. `heavy_fields_status` is\n`\"ok\"` when the public-archive fetch succeeded and `\"unavailable\"` when it\nfailed (graceful degradation per ADR 0029 — the light slice is always\nreturned). Per ADR 0033 there is no separate \"advanced\" view; the wrapper\nalways carries the full heavy payload when available.",
        "operationId": "get_transaction",
        "parameters": [
          {
            "name": "hash",
            "in": "path",
            "description": "Transaction hash (64-char hex; uppercase or lowercase accepted, normalised server-side)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Transaction detail (light + heavy block)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/E3Response_TransactionDetailLight"
                }
              }
            }
          },
          "400": {
            "description": "Invalid hash",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "Transaction not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "AccountBalance": {
        "type": "object",
        "description": "Native rows have `null` `asset_code` / `asset_issuer`; classic credit rows have\nboth; Soroban (type-3) token rows carry `contract_id` + on-chain `name`/`symbol`\ninstead (the account portfolio includes Soroban balances, task 0331 Option C).",
        "required": ["type", "balance", "decimals", "last_updated_ledger"],
        "properties": {
          "asset_code": {
            "type": ["string", "null"]
          },
          "asset_issuer": {
            "type": ["string", "null"]
          },
          "asset_type_name": {
            "type": ["string", "null"],
            "description": "Horizon-style label from `asset_type_name`: `native` | `credit_alphanum4`\n| `credit_alphanum12` for classic. Soroban (type-3) rows are also returned\n— identify them via `contract_id`, not this label."
          },
          "balance": {
            "type": "string",
            "description": "RAW integer balance as a string (`Int128`) — scale by `decimals` (task 0331\nOption C: one convention for all asset types; classic `decimals` = 7). The\naccount portfolio now includes Soroban (type-3) token balances too."
          },
          "contract_id": {
            "type": ["string", "null"],
            "description": "C-strkey of the Soroban token's contract (type-3 only) — the identity +\nlink target (`/assets/${contract_id}`). `null` for native / classic."
          },
          "decimals": {
            "type": "integer",
            "format": "int32",
            "description": "Display decimals — 7 for classic, on-chain `METADATA` for Soroban tokens.",
            "minimum": 0
          },
          "last_updated_ledger": {
            "type": "integer",
            "format": "int64"
          },
          "name": {
            "type": ["string", "null"],
            "description": "Asset display `name`, from two disjoint sources by asset type: classic /\nnative → off-chain SEP-1 enrichment (`asset_enrichment`, only ~3% of classic\nassets carry one); Soroban (type-3) → on-chain `METADATA` (e.g. \"USDC-EURC\nSoroswap LP Token\", 100% coverage). Distinct from the `symbol` ticker.\n`null` when neither source has a name."
          },
          "symbol": {
            "type": ["string", "null"],
            "description": "On-chain token `symbol` (type-3, from `METADATA`, e.g. \"SMOL\") — the short\nticker. `null` for native / classic (they carry `asset_code`)."
          },
          "type": {
            "type": "integer",
            "format": "int32",
            "description": "Raw SMALLINT — stable across label renames."
          }
        }
      },
      "AccountDetailResponse": {
        "type": "object",
        "required": [
          "account_id",
          "sequence_number",
          "balances",
          "first_seen_ledger",
          "last_seen_ledger",
          "deleted"
        ],
        "properties": {
          "account_id": {
            "type": "string"
          },
          "balances": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AccountBalance"
            }
          },
          "deleted": {
            "type": "boolean",
            "description": "`true` when the account was removed from the ledger via `account_merge`\nand never re-funded (its last lifecycle event is the merge). Derived,\nnot stored."
          },
          "first_seen_ledger": {
            "type": "integer",
            "format": "int64"
          },
          "home_domain": {
            "type": ["string", "null"]
          },
          "last_seen_ledger": {
            "type": "integer",
            "format": "int64"
          },
          "sequence_number": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "AccountListItem": {
        "type": "object",
        "description": "One row of `GET /v1/accounts`. Identity + native (XLM) balance + the\nfirst/last-seen activity window + `home_domain`. Ordered by\n`last_seen_ledger` (the only indexed sort). `xlm_balance` is the native\nbalance from the unified `balances` table; `null` if no native row exists.",
        "required": ["account_id", "last_seen_ledger", "first_seen_ledger"],
        "properties": {
          "account_id": {
            "type": "string"
          },
          "first_seen_ledger": {
            "type": "integer",
            "format": "int64"
          },
          "home_domain": {
            "type": ["string", "null"]
          },
          "last_seen_ledger": {
            "type": "integer",
            "format": "int64"
          },
          "xlm_balance": {
            "type": ["string", "null"],
            "description": "Native (XLM) balance as a RAW `Int128` stroop string (human value =\n÷10⁷); the frontend scales by 7 decimals — same raw-amount contract as\nthe account-detail balances."
          }
        }
      },
      "AccountTransactionItem": {
        "type": "object",
        "description": "Slim — `inner_tx_hash` lives on `/v1/transactions` only.",
        "required": [
          "hash",
          "ledger_sequence",
          "application_order",
          "source_account",
          "fee_charged",
          "successful",
          "operation_count",
          "has_soroban",
          "operation_types",
          "values",
          "created_at"
        ],
        "properties": {
          "application_order": {
            "type": "integer",
            "format": "int32",
            "description": "1-based position in ledger."
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "fee_charged": {
            "type": "integer",
            "format": "int64",
            "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
          },
          "has_soroban": {
            "type": "boolean"
          },
          "hash": {
            "type": "string",
            "description": "64-char lowercase hex."
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          },
          "operation_count": {
            "type": "integer",
            "format": "int32"
          },
          "operation_types": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "source_account": {
            "type": "string"
          },
          "successful": {
            "type": "boolean"
          },
          "values": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TransactionValue"
            },
            "description": "Net-settled \"value moved\" per asset the transaction touched (task 0393);\nsee `TransactionValue`. Raw amounts + `decimals` — the frontend scales."
          }
        }
      },
      "AssetDetailResponse": {
        "allOf": [
          {
            "type": "object",
            "description": "Asset row returned by list and detail. Surfaces both the decoded\n`asset_type_name` (SQL `token_asset_type_name()`) and the raw `asset_type`\nSMALLINT — canonical SQL `08_get_assets_list.sql` projection.",
            "required": ["id", "asset_type", "decimals"],
            "properties": {
              "asset_code": {
                "type": ["string", "null"]
              },
              "asset_type": {
                "type": "integer",
                "format": "int32",
                "description": "Raw SMALLINT (0=native, 1=classic_credit, 3=soroban). 2 (`sac`) is retired."
              },
              "asset_type_name": {
                "type": ["string", "null"],
                "description": "`native | classic_credit | soroban` (ADR 0051 — `sac` retired). `null`\nonly on schema drift."
              },
              "contract_id": {
                "type": ["string", "null"],
                "description": "Soroban contract StrKey (`C…`) — set ONLY for `soroban` (type=3), where\nthe contract IS the asset. `null` for native / classic_credit (a wrapping\nSAC's address rides in `sac_contract_id`)."
              },
              "decimals": {
                "type": "integer",
                "format": "int32",
                "description": "Display decimals — on-chain `METADATA` for Soroban tokens, else 7\n(Stellar classic precision). Load-bearing for amount rendering.",
                "minimum": 0
              },
              "holder_count": {
                "type": ["integer", "null"],
                "format": "int32",
                "description": "Active-holder count (`amount > 0`) from the unified `balances` aggregate\n(all asset types — trustline holders + contract holders). `null` = no data."
              },
              "icon_url": {
                "type": ["string", "null"]
              },
              "id": {
                "type": "string",
                "description": "Canonical identifier — the single token usable as `/assets/{id}`:\nthe reserved `native` token for native XLM, the contract StrKey\n(`C…`) for contract-backed assets (SAC / Soroban), otherwise the\n`CODE-ISSUER` composite (classic credit, e.g. `USDC-GA…`). Replaces\nthe dropped numeric surrogate (PR #175): assets are keyed on\n`(asset_type, asset_code, issuer_id, contract_id)`, with no surrogate."
              },
              "issuer": {
                "type": ["string", "null"]
              },
              "issuer_home_domain": {
                "type": ["string", "null"],
                "description": "The issuer account's on-chain `home_domain` (SEP-1 anchor domain), e.g.\n`centre.io`. Read from the same `accounts` key-seek that resolves\n`issuer`, so it costs no extra query (task 0450). `null` for native /\nSoroban-native assets (no issuer) and for issuers that never set one —\nmost do not.\n\nNOT a verified identity: the account holder sets this field itself and\nnothing checks it. Render it as a claim, never as a badge implying we\nconfirmed the domain owns the asset.\n\nDistinct from the detail response's `home_page`, which is fetched from\nthe issuer's `stellar.toml` at request time."
              },
              "name": {
                "type": ["string", "null"]
              },
              "sac_contract_id": {
                "type": ["string", "null"],
                "description": "SAC facet (ADR 0051): the wrapping Stellar Asset Contract's `C…` StrKey\nfor a native / classic_credit asset that has one, re-derived on read from\n`code:issuer` (never stored). `null` when the asset has no observed SAC."
              },
              "sac_deployed": {
                "type": ["boolean", "null"],
                "description": "Whether `sac_contract_id`'s SAC is deployed on-chain (ADR 0051). `null`\nwhen the asset has no SAC; `false` = reserved-but-un-deployed address\n(render the contract link non-clickable)."
              },
              "symbol": {
                "type": ["string", "null"],
                "description": "On-chain SEP-41 token symbol (Soroban `METADATA`). `null` for classic\n(use `asset_code`) and native."
              },
              "total_supply": {
                "type": ["string", "null"],
                "description": "Total supply as a RAW integer string (`Int128`) — scale by `decimals` for\ndisplay (task 0331 Option C: one convention for ALL asset types; classic\n`decimals` is 7). E.g. `\"63836094715548\"`. `null` = no balance data\n(a token/asset with no holders). Sourced from `balance_aggregates` over the\nunified `balances` table."
              }
            }
          },
          {
            "type": "object",
            "properties": {
              "deployed_at_ledger": {
                "type": ["integer", "null"],
                "format": "int64",
                "description": "`soroban_contracts.deployed_at_ledger` — `null` for classic / native."
              },
              "description": {
                "type": ["string", "null"]
              },
              "home_page": {
                "type": ["string", "null"]
              }
            }
          }
        ],
        "description": "Detail response. `description` is populated from the issuer stellar.toml\n`CURRENCIES[].desc` field; `home_page` is populated from\n`DOCUMENTATION.ORG_URL` (SEP-1 has no per-currency `home_page` field —\nthe org URL is the closest semantic match and preserves backward\ncompatibility with the previous DB-sourced column). Both default to\n`null` for native XLM, assets without an issuer, issuers without an\non-chain `home_domain`, fetch failures, and stellar.toml files with\nno matching `[[CURRENCIES]]` entry."
      },
      "AssetItem": {
        "type": "object",
        "description": "Asset row returned by list and detail. Surfaces both the decoded\n`asset_type_name` (SQL `token_asset_type_name()`) and the raw `asset_type`\nSMALLINT — canonical SQL `08_get_assets_list.sql` projection.",
        "required": ["id", "asset_type", "decimals"],
        "properties": {
          "asset_code": {
            "type": ["string", "null"]
          },
          "asset_type": {
            "type": "integer",
            "format": "int32",
            "description": "Raw SMALLINT (0=native, 1=classic_credit, 3=soroban). 2 (`sac`) is retired."
          },
          "asset_type_name": {
            "type": ["string", "null"],
            "description": "`native | classic_credit | soroban` (ADR 0051 — `sac` retired). `null`\nonly on schema drift."
          },
          "contract_id": {
            "type": ["string", "null"],
            "description": "Soroban contract StrKey (`C…`) — set ONLY for `soroban` (type=3), where\nthe contract IS the asset. `null` for native / classic_credit (a wrapping\nSAC's address rides in `sac_contract_id`)."
          },
          "decimals": {
            "type": "integer",
            "format": "int32",
            "description": "Display decimals — on-chain `METADATA` for Soroban tokens, else 7\n(Stellar classic precision). Load-bearing for amount rendering.",
            "minimum": 0
          },
          "holder_count": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "Active-holder count (`amount > 0`) from the unified `balances` aggregate\n(all asset types — trustline holders + contract holders). `null` = no data."
          },
          "icon_url": {
            "type": ["string", "null"]
          },
          "id": {
            "type": "string",
            "description": "Canonical identifier — the single token usable as `/assets/{id}`:\nthe reserved `native` token for native XLM, the contract StrKey\n(`C…`) for contract-backed assets (SAC / Soroban), otherwise the\n`CODE-ISSUER` composite (classic credit, e.g. `USDC-GA…`). Replaces\nthe dropped numeric surrogate (PR #175): assets are keyed on\n`(asset_type, asset_code, issuer_id, contract_id)`, with no surrogate."
          },
          "issuer": {
            "type": ["string", "null"]
          },
          "issuer_home_domain": {
            "type": ["string", "null"],
            "description": "The issuer account's on-chain `home_domain` (SEP-1 anchor domain), e.g.\n`centre.io`. Read from the same `accounts` key-seek that resolves\n`issuer`, so it costs no extra query (task 0450). `null` for native /\nSoroban-native assets (no issuer) and for issuers that never set one —\nmost do not.\n\nNOT a verified identity: the account holder sets this field itself and\nnothing checks it. Render it as a claim, never as a badge implying we\nconfirmed the domain owns the asset.\n\nDistinct from the detail response's `home_page`, which is fetched from\nthe issuer's `stellar.toml` at request time."
          },
          "name": {
            "type": ["string", "null"]
          },
          "sac_contract_id": {
            "type": ["string", "null"],
            "description": "SAC facet (ADR 0051): the wrapping Stellar Asset Contract's `C…` StrKey\nfor a native / classic_credit asset that has one, re-derived on read from\n`code:issuer` (never stored). `null` when the asset has no observed SAC."
          },
          "sac_deployed": {
            "type": ["boolean", "null"],
            "description": "Whether `sac_contract_id`'s SAC is deployed on-chain (ADR 0051). `null`\nwhen the asset has no SAC; `false` = reserved-but-un-deployed address\n(render the contract link non-clickable)."
          },
          "symbol": {
            "type": ["string", "null"],
            "description": "On-chain SEP-41 token symbol (Soroban `METADATA`). `null` for classic\n(use `asset_code`) and native."
          },
          "total_supply": {
            "type": ["string", "null"],
            "description": "Total supply as a RAW integer string (`Int128`) — scale by `decimals` for\ndisplay (task 0331 Option C: one convention for ALL asset types; classic\n`decimals` is 7). E.g. `\"63836094715548\"`. `null` = no balance data\n(a token/asset with no holders). Sourced from `balance_aggregates` over the\nunified `balances` table."
          }
        }
      },
      "AssetTransactionItem": {
        "type": "object",
        "description": "Transaction row for `/assets/:id/transactions`. Pure-DB; mirrors\ncanonical SQL `10_get_assets_transactions.sql`.",
        "required": [
          "hash",
          "ledger_sequence",
          "source_account",
          "successful",
          "fee_charged",
          "created_at",
          "operation_count",
          "has_soroban",
          "operation_types"
        ],
        "properties": {
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "fee_charged": {
            "type": "integer",
            "format": "int64",
            "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
          },
          "has_soroban": {
            "type": "boolean"
          },
          "hash": {
            "type": "string"
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          },
          "operation_count": {
            "type": "integer",
            "format": "int32"
          },
          "operation_types": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Distinct `op_type_name(...)` labels for every op in the tx, sorted asc."
          },
          "source_account": {
            "type": "string"
          },
          "successful": {
            "type": "boolean"
          }
        }
      },
      "ChartDataPoint": {
        "type": "object",
        "description": "One row from the chart endpoint. Shape pinned to canonical SQL\n`21_get_liquidity_pools_chart.sql`. `tvl` is \"TVL at close of bucket\"\n(last value); `volume` and `fee_revenue` are SUM (cumulative within\nthe bucket).",
        "required": ["bucket", "samples_in_bucket"],
        "properties": {
          "bucket": {
            "type": "string",
            "format": "date-time"
          },
          "fee_revenue": {
            "type": ["string", "null"]
          },
          "samples_in_bucket": {
            "type": "integer",
            "format": "int64"
          },
          "tvl": {
            "type": ["string", "null"]
          },
          "volume": {
            "type": ["string", "null"]
          }
        }
      },
      "ChartResponse": {
        "type": "object",
        "description": "`GET /v1/liquidity-pools/:id/chart` response.",
        "required": ["pool_id", "interval", "from", "to", "data_points"],
        "properties": {
          "data_points": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ChartDataPoint"
            }
          },
          "from": {
            "type": "string",
            "format": "date-time"
          },
          "interval": {
            "type": "string"
          },
          "pool_id": {
            "type": "string",
            "description": "Echoed pool ID — SEP-23 strkey (`L...`, 56 chars), same form the\nclient supplied in the path."
          },
          "to": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "ContractDetailResponse": {
        "type": "object",
        "required": ["contract_id", "is_sac", "stats"],
        "properties": {
          "contract_id": {
            "type": "string"
          },
          "contract_type": {
            "type": ["integer", "null"],
            "format": "int32"
          },
          "contract_type_name": {
            "type": ["string", "null"]
          },
          "deployed_at_ledger": {
            "type": ["integer", "null"],
            "format": "int64"
          },
          "deployer": {
            "type": ["string", "null"]
          },
          "is_sac": {
            "type": "boolean"
          },
          "stats": {
            "$ref": "#/components/schemas/ContractStats"
          },
          "upgradeable": {
            "type": ["boolean", "null"],
            "description": "Task 0327: contract mutability, 3-state.\n- `Some(true)` → **Upgradeable**: the current WASM imports\n  `update_current_contract_wasm` (a self-upgrade path).\n- `Some(false)` → **Immutable/frozen**: it cannot upgrade itself (a SAC\n  has no WASM and is always `Some(false)`).\n- `None` → **Unknown**: the WASM interface hasn't been parsed yet (stub /\n  pre-0327 row) — the frontend renders no chip.\n\nDerived from the WASM at parse time\n(`wasm_interface_metadata.metadata.upgradeable`), not from a ledger flag\n(none exists)."
          },
          "wasm_hash": {
            "type": ["string", "null"]
          },
          "wasm_uploaded_at_ledger": {
            "type": ["integer", "null"],
            "format": "int64"
          }
        }
      },
      "ContractFunctionParam": {
        "type": "object",
        "description": "One parameter on a Soroban contract function signature.",
        "required": ["name", "type_name"],
        "properties": {
          "name": {
            "type": "string"
          },
          "type_name": {
            "type": "string"
          }
        }
      },
      "ContractFunctionSig": {
        "type": "object",
        "description": "A single public function signature extracted from a Soroban contract's\nWASM spec. Mirror of `xdr_parser::types::ContractFunction`, which is\nthe indexer-side source of the persisted shape.",
        "required": ["name", "doc", "inputs", "outputs"],
        "properties": {
          "doc": {
            "type": "string",
            "description": "Documentation string; may be empty."
          },
          "inputs": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ContractFunctionParam"
            }
          },
          "name": {
            "type": "string"
          },
          "outputs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Output type names; empty array == void return."
          }
        }
      },
      "ContractInterfaceMetadata": {
        "type": "object",
        "description": "Soroban contract interface metadata persisted in\n`wasm_interface_metadata.metadata` (JSONB). Field shape mirrors the\nindexer's `xdr_parser::types::ContractInterface` exactly — the API\nhands the same JSON object to clients that the indexer wrote.",
        "required": ["functions", "wasm_byte_len"],
        "properties": {
          "functions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ContractFunctionSig"
            }
          },
          "wasm_byte_len": {
            "type": "integer",
            "format": "int64",
            "description": "Raw WASM byte length (informational)."
          }
        }
      },
      "ContractListItem": {
        "type": "object",
        "description": "One row of `GET /v1/contracts`. Identity + classification + deploy\nprovenance + a 7-day activity signal. All fields come straight from\n`soroban_contracts` (+ a deployer join and a windowed invocation count);\nnullable fields are `None` until the contract's deploy is observed.",
        "required": ["contract_id", "is_sac", "recent_invocations"],
        "properties": {
          "contract_id": {
            "type": "string"
          },
          "contract_type": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "Raw SMALLINT class (0=token, 1=other, 2=nft, 3=fungible). `null`\nuntil deployment is observed."
          },
          "contract_type_name": {
            "type": ["string", "null"],
            "description": "`token | other | nft | fungible`. `null` only on schema drift / no type."
          },
          "deployed_at_ledger": {
            "type": ["integer", "null"],
            "format": "int64",
            "description": "Ledger the deploy was observed at; `null` until then."
          },
          "deployer": {
            "type": ["string", "null"],
            "description": "Deployer account G-strkey; `null` until the deploy op is observed."
          },
          "is_sac": {
            "type": "boolean",
            "description": "Stellar Asset Contract flag (stored, not derived from `contract_type`)."
          },
          "recent_invocations": {
            "type": "integer",
            "format": "int64",
            "description": "Invocation count over the last 7 days (windowed; matches the detail\n`ContractStats.recent_invocations` semantics)."
          }
        }
      },
      "ContractStats": {
        "type": "object",
        "required": [
          "recent_invocations",
          "recent_unique_callers",
          "recent_events",
          "stats_window"
        ],
        "properties": {
          "recent_events": {
            "type": "integer",
            "format": "int64",
            "description": "Event count in the same window as `recent_invocations` (NOT the full\n`/events` history — that endpoint pages all events with no time bound).\n`count()`s the `soroban_events` rows (one row per event) written from the\nparser event stream (diagnostics dropped at parse; System + Contract kept)."
          },
          "recent_invocations": {
            "type": "integer",
            "format": "int64"
          },
          "recent_unique_callers": {
            "type": "integer",
            "format": "int64"
          },
          "stats_window": {
            "type": "string",
            "description": "Echoed window label (e.g. `\"7 days\"`) so the UI can label \"last N days\"."
          }
        }
      },
      "DecompiledResponse": {
        "type": "object",
        "description": "Response of `GET /v1/contracts/{contract_id}/decompiled` (task 0465).\n\nSource is reconstructed on demand by the pinned `soroban-ret` crate —\nexperimental by nature: unrecovered values surface as explicit `todo!()`\nholes in the Rust text. The marker counts measure completeness, not\ncorrectness (a hole-free function can still be wrong); the frontend\nkeeps its permanent \"auto-reconstructed\" notice regardless.",
        "required": [
          "contract_id",
          "wasm_hash",
          "representation",
          "source",
          "soroban_ret_version"
        ],
        "properties": {
          "contract_id": {
            "type": "string"
          },
          "functions": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "`pub fn` count in the emitted Rust; `null` for WAT.",
            "minimum": 0
          },
          "representation": {
            "type": "string",
            "description": "What `source` contains: `rust` or `wat`."
          },
          "rust_error": {
            "type": ["string", "null"],
            "description": "Set when `rust` was requested but emission failed — `source` then\ncarries the WAT fallback."
          },
          "sdk_version": {
            "type": ["string", "null"],
            "description": "Soroban SDK version from the binary's `contractmetav0`, when present."
          },
          "soroban_ret_version": {
            "type": "string",
            "description": "Decompiler version that produced `source`."
          },
          "source": {
            "type": "string"
          },
          "todo_holes": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "`todo!()` marker count (unrecovered values); `null` for WAT.",
            "minimum": 0
          },
          "unknown_vars": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "Distinct `var_N` identifiers (unrecovered names); `null` for WAT.",
            "minimum": 0
          },
          "wasm_hash": {
            "type": "string",
            "description": "Lowercase hex hash of the decompiled binary — the response is\nimmutable per (`wasm_hash`, `soroban_ret_version`)."
          }
        }
      },
      "E3HeavyFields": {
        "type": "object",
        "description": "E3 (`GET /transactions/:hash`) — fields sourced from XDR parse.\n\nReturned as the `heavy` block inside `E3Response<TransactionDetailLight>`.\nOn upstream fetch failure the caller substitutes `None` and\n`merge_e3_response` sets `heavy_fields_status = \"unavailable\"`.",
        "required": [
          "signatures",
          "diagnostic_events",
          "contract_events",
          "operations"
        ],
        "properties": {
          "contract_events": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/XdrEventDto"
            },
            "description": "The consensus event stream: `contract` + `system` events from the\ntx-level and per-operation containers. This — and only this — is what\nCAP-67 / `getEvents` mean by \"the events of a transaction\"."
          },
          "diagnostic_events": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/XdrEventDto"
            },
            "description": "The host-VM debug channel (`v4.diagnostic_events`): the `fn_call` /\n`fn_return` trace, `core_metrics` counters, and — when diagnostic mode\nis on, which is always for the archive — a byte-identical COPY of every\nconsensus event above. Not hashed into consensus, and CAP-67's own\nevent stream (`getEvents`) does not carry it at all. Presenting these\nalongside `contract_events` as one list shows the copies as extra\nevents; they are one channel about the other, not a continuation of it."
          },
          "envelope_xdr": {
            "type": ["string", "null"],
            "description": "Base64-encoded `TransactionEnvelope`."
          },
          "fee_bump_source": {
            "type": ["string", "null"],
            "description": "Fee-bump envelope `feeSource` StrKey when the outer tx is a fee-bump."
          },
          "memo": {
            "type": ["string", "null"],
            "description": "Memo payload rendered as string (hex for hash/return, decimal for id)."
          },
          "memo_type": {
            "type": ["string", "null"],
            "description": "Memo type as ASCII tag (`\"text\"`, `\"id\"`, `\"hash\"`, `\"return\"`, `\"none\"`)."
          },
          "operation_tree": {
            "description": "Nested Soroban invocation tree, derived from `result_meta_xdr` at\nextraction time."
          },
          "operations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/XdrOperationDto"
            },
            "description": "Operations with full XDR-decoded details (type-specific JSON)."
          },
          "result_code": {
            "type": ["string", "null"],
            "description": "Transaction result code (e.g. `\"txSuccess\"`, `\"txFailed\"`).\n`None` only when the transaction had a parse error."
          },
          "result_meta_xdr": {
            "type": ["string", "null"],
            "description": "Base64-encoded `TransactionMeta` — the ledger-entry changes (who held\nwhat before/after). The 0046 spec originally withheld it; reversed by\ntask 0460 #13: it is the one raw layer the page could not show, and\nthe raw-data section renders every XDR blob with a Lab deep link."
          },
          "result_xdr": {
            "type": ["string", "null"],
            "description": "Base64-encoded `TransactionResult`."
          },
          "signatures": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SignatureDto"
            },
            "description": "Transaction-level signatures (hint + signature bytes hex-encoded)."
          }
        }
      },
      "E3Response_TransactionDetailLight": {
        "allOf": [
          {
            "type": "object",
            "description": "DB-sourced light slice for the transaction detail endpoint.\n\nComposed with `E3HeavyFields` via `merge_e3_response` (task 0150). All\nXDR-sourced fields (memo, result_code, signatures, events, operation\ndetails, envelope_xdr/result_xdr, operation_tree) live in `heavy`.",
            "required": [
              "hash",
              "ledger_sequence",
              "application_order",
              "fee_charged",
              "successful",
              "operation_count",
              "has_soroban",
              "created_at",
              "parse_error",
              "operations",
              "participants",
              "soroban_events",
              "soroban_invocations"
            ],
            "properties": {
              "application_order": {
                "type": "integer",
                "format": "int32",
                "description": "1-based position of this transaction within its ledger."
              },
              "created_at": {
                "type": "string",
                "format": "date-time"
              },
              "fee_charged": {
                "type": "integer",
                "format": "int64",
                "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
              },
              "has_soroban": {
                "type": "boolean"
              },
              "hash": {
                "type": "string",
                "description": "Transaction hash (64-char lowercase hex)."
              },
              "inner_tx_hash": {
                "type": ["string", "null"],
                "description": "Inner-transaction hash (64-char hex) for fee-bump envelopes, `null` otherwise."
              },
              "ledger_sequence": {
                "type": "integer",
                "format": "int64"
              },
              "operation_count": {
                "type": "integer",
                "format": "int32"
              },
              "operations": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/OperationItem"
                }
              },
              "parse_error": {
                "type": "boolean",
                "description": "`true` when the XDR parser encountered an error for this transaction."
              },
              "participants": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Accounts touched by this transaction. Populated only when\n`heavy_fields_status = \"unavailable\"`; otherwise `[]` and consumers\nshould rely on the heavy block."
              },
              "soroban_events": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/EventAppearanceItem"
                },
                "description": "Soroban event appearance index rows. Same fallback semantics as\n`participants`. Full topics + data live in `heavy.contract_events`."
              },
              "soroban_invocations": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/InvocationAppearanceItem"
                },
                "description": "Soroban invocation appearance index rows. Same fallback semantics\nas `participants`. Full call hierarchy lives in `heavy.operation_tree`."
              },
              "source_account": {
                "type": ["string", "null"],
                "description": "`null` for Variant A `parse_error` transactions whose envelope\ncould not be decoded (lore-0209)."
              },
              "successful": {
                "type": "boolean"
              }
            }
          },
          {
            "type": "object",
            "required": ["heavy_fields_status"],
            "properties": {
              "heavy": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/E3HeavyFields"
                  }
                ]
              },
              "heavy_fields_status": {
                "$ref": "#/components/schemas/HeavyFieldsStatus"
              }
            }
          }
        ],
        "description": "Merged E3 response: DB light fields + optional XDR heavy fields.\n\n`heavy_fields_status` = `Ok` when `heavy` is `Some`, `Unavailable` when\nthe public-archive fetch failed and the caller degraded gracefully."
      },
      "EntityType": {
        "type": "string",
        "description": "Entity discriminator. Closed allowlist used by the `type=` filter\nand the `entity_type` field on every hit / redirect.",
        "enum": ["transaction", "account", "asset", "contract", "nft", "pool"]
      },
      "ErrorEnvelope": {
        "type": "object",
        "description": "Canonical error response body.\n\nIntentionally simpler than RFC 7807 (`problem+json`) — see ADR 0008.\nMachine-readable `code` drives client behaviour; `message` is a\nhuman-friendly description; `details` carries optional structured\ncontext (field-level validation errors, etc.).",
        "required": ["code", "message"],
        "properties": {
          "code": {
            "type": "string",
            "description": "Stable machine-readable error code, e.g. `\"invalid_cursor\"`."
          },
          "details": {
            "description": "Optional structured details. Shape is error-specific."
          },
          "message": {
            "type": "string",
            "description": "Human-readable error description."
          }
        }
      },
      "EventAppearanceItem": {
        "type": "object",
        "required": ["contract_id", "ledger_sequence", "created_at"],
        "properties": {
          "contract_id": {
            "type": "string"
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "EventItem": {
        "type": "object",
        "description": "One row per event — the full-content `soroban_events` table stores one\nrow per event (no appearance-fold expansion).",
        "required": [
          "transaction_hash",
          "ledger_sequence",
          "transaction_id",
          "successful",
          "created_at",
          "event_type",
          "topics",
          "data"
        ],
        "properties": {
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "data": {},
          "event_type": {
            "type": "string"
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          },
          "successful": {
            "type": "boolean"
          },
          "topics": {
            "type": "array",
            "items": {}
          },
          "transaction_hash": {
            "type": "string"
          },
          "transaction_id": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "HeavyFieldsStatus": {
        "type": "string",
        "description": "Indicates whether the XDR-sourced fields were loaded successfully.",
        "enum": ["ok", "unavailable"]
      },
      "InterfaceResponse": {
        "type": "object",
        "description": "`interface_metadata` is `null` for SAC / pre-upload / stub rows;\nstubs (task 0153) are filtered at the SQL layer so they don't leak.",
        "required": ["contract_id"],
        "properties": {
          "contract_id": {
            "type": "string"
          },
          "interface_metadata": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ContractInterfaceMetadata"
              }
            ]
          },
          "wasm_hash": {
            "type": ["string", "null"]
          }
        }
      },
      "InvocationAppearanceItem": {
        "type": "object",
        "required": ["contract_id", "ledger_sequence", "created_at"],
        "properties": {
          "caller_account": {
            "type": ["string", "null"],
            "description": "Root caller G-StrKey. Per ADR 0034 nested-call hierarchy is XDR-only."
          },
          "contract_id": {
            "type": "string"
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "InvocationItem": {
        "type": "object",
        "required": [
          "transaction_hash",
          "ledger_sequence",
          "created_at",
          "successful"
        ],
        "properties": {
          "caller_account": {
            "type": ["string", "null"]
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          },
          "successful": {
            "type": "boolean"
          },
          "transaction_hash": {
            "type": "string"
          }
        }
      },
      "LedgerDetailResponse": {
        "type": "object",
        "description": "Detail response for `GET /v1/ledgers/:sequence`.\n\nHeader columns are the same shape as `LedgerListItem`. `prev_sequence` /\n`next_sequence` identify the adjacent indexed ledgers in `sequence`\norder (computed via LATERAL on the `ledgers` PK) and double as a\nchain-position signal — when `next_sequence` is null the ledger is the\nchain head, which drives the short-TTL Cache-Control branch in the\nhandler. `transactions` is the embedded paginated list of\n`TransactionListItem` rows, served DB-only (memo and other heavy\nfields belong to the transaction detail endpoint, not list rows).",
        "required": [
          "sequence",
          "hash",
          "closed_at",
          "protocol_version",
          "transaction_count",
          "base_fee",
          "transactions"
        ],
        "properties": {
          "base_fee": {
            "type": "integer",
            "format": "int64"
          },
          "closed_at": {
            "type": "string",
            "format": "date-time"
          },
          "hash": {
            "type": "string",
            "description": "Ledger hash (64-char lowercase hex)."
          },
          "next_sequence": {
            "type": ["integer", "null"],
            "format": "int64",
            "description": "Sequence number of the ledger that closed immediately after this\none. `null` at the chain head (this is the most recent ledger we\nhave indexed)."
          },
          "prev_sequence": {
            "type": ["integer", "null"],
            "format": "int64",
            "description": "Sequence number of the ledger that closed immediately before this\none. `null` at the chain tail (no earlier ledger persisted)."
          },
          "protocol_version": {
            "type": "integer",
            "format": "int32"
          },
          "sequence": {
            "type": "integer",
            "format": "int64"
          },
          "transaction_count": {
            "type": "integer",
            "format": "int32"
          },
          "transactions": {
            "$ref": "#/components/schemas/Paginated_TransactionListItem",
            "description": "Paginated linked transactions, DB-only `TransactionListItem` rows."
          }
        }
      },
      "LedgerListItem": {
        "type": "object",
        "description": "Slim ledger row returned in the list endpoint and reused inside the\ndetail response as the header block.",
        "required": [
          "sequence",
          "hash",
          "closed_at",
          "protocol_version",
          "transaction_count",
          "base_fee"
        ],
        "properties": {
          "base_fee": {
            "type": "integer",
            "format": "int64"
          },
          "closed_at": {
            "type": "string",
            "format": "date-time"
          },
          "hash": {
            "type": "string",
            "description": "Ledger hash (64-char lowercase hex)."
          },
          "protocol_version": {
            "type": "integer",
            "format": "int32"
          },
          "sequence": {
            "type": "integer",
            "format": "int64"
          },
          "transaction_count": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "NetworkStats": {
        "type": "object",
        "description": "Top-level chain overview returned by `GET /v1/network/stats`.\n\n`total_accounts` and `total_contracts` are deduped read-time counts —\nsee the per-field docs for the exactness each one carries. (They were\n`system.tables.total_rows` planner estimates until 0420, which counted\nunmerged ReplacingMergeTree duplicates and over-reported; that source is\ngone.) `latest_ledger_closed_at` is `None` only on a cold-bootstrap\ncluster where no ledger has been indexed yet.\n\n`generated_at` is the wall-clock time the underlying SELECT ran on\nthe DB. Cache hits keep the original value, so frontend can derive\ntwo distinct signals without confusing them with cache age:\n\n* indexer-health lag = `generated_at - latest_ledger_closed_at`\n* data staleness (\"info from N seconds ago\") = `Date.now() - generated_at`",
        "required": [
          "tps_60s",
          "total_accounts",
          "total_contracts",
          "latest_ledger_sequence",
          "generated_at"
        ],
        "properties": {
          "generated_at": {
            "type": "string",
            "format": "date-time",
            "description": "Wall-clock time the underlying SELECT ran on the DB (`NOW()` at\nfetch time). Preserved across cache hits — frontend uses this to\ndistinguish indexer-health lag from cache-age staleness."
          },
          "latest_ledger_closed_at": {
            "type": ["string", "null"],
            "format": "date-time",
            "description": "Close time of the newest ledger in the database. `null` only\nwhen no ledgers are indexed (cold-bootstrap cluster)."
          },
          "latest_ledger_sequence": {
            "type": "integer",
            "format": "int64",
            "description": "Sequence of the newest ledger in the database (ordered by\n`closed_at DESC`). `0` sentinel indicates an empty cluster (no\nledger 0 exists in Stellar)."
          },
          "total_accounts": {
            "type": "integer",
            "format": "int64",
            "description": "Indexed account count: `count()` over `accounts_recent`, the deduped\nMV the `/accounts` list also pages from — so this KPI and that list\nagree. Exact to ±1 vs `accounts FINAL`, modulo the ~2-minute MV\nrefresh. Safe to render as a total."
          },
          "total_contracts": {
            "type": "integer",
            "format": "int64",
            "description": "Indexed Soroban contract count: `count() FROM soroban_contracts FINAL`.\nExact — `FINAL` is affordable on a table this size."
          },
          "tps_60s": {
            "type": "number",
            "format": "double",
            "description": "Transactions per second over a 60s rolling window. Computed as\n`SUM(ledgers.transaction_count)` divided by the actual span\nbetween MIN/MAX `closed_at` in the window. Stable on partial /\nsingle-ledger windows (NULLIF guards zero-span)."
          }
        }
      },
      "NftDetailResponse": {
        "allOf": [
          {
            "type": "object",
            "description": "One NFT row. Same shape on `GET /v1/nfts` list rows and as the\nflattened core of `GET /v1/nfts/:id` (which adds `metadata`). Pinned\nto canonical SQL `15_get_nfts_list.sql` for the column projection.\n\nThe numeric surrogate `id` was dropped (task 0243 NFT slice): the\nexternal NFT identity is the composite `(contract_id, token_id)` per\ntask 0264 Phase 8a, and ClickHouse — the production datastore — has no\nsurrogate on `nfts` at all (`ORDER BY (contract_id, token_id)`). This\nmirrors the assets `:id`-surrogate drop (PR #175).",
            "required": ["contract_id", "token_id"],
            "properties": {
              "collection_name": {
                "type": ["string", "null"]
              },
              "contract_id": {
                "type": "string",
                "description": "Contract C-StrKey resolved via `soroban_contracts` join."
              },
              "last_seen_ledger": {
                "type": ["integer", "null"],
                "format": "int64",
                "description": "Most recent ledger where ownership state changed\n(`nfts.current_owner_ledger`)."
              },
              "media_url": {
                "type": ["string", "null"]
              },
              "minted_at_ledger": {
                "type": ["integer", "null"],
                "format": "int64"
              },
              "name": {
                "type": ["string", "null"]
              },
              "owner_account": {
                "type": ["string", "null"],
                "description": "Current owner G-StrKey, or `null` for burned NFTs (ADR 0037 §13)."
              },
              "token_id": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "required": ["metadata"],
            "properties": {
              "metadata": {
                "type": ["object", "null"],
                "description": "Off-chain JSON metadata fetched at request time. Always present\non the wire — `null` indicates fetch failure / unsupported\ncontent-type, not field absence."
              }
            }
          }
        ],
        "description": "Detail response for `GET /v1/nfts/:id`. The `NftItem` fields are\nflattened in (same shape as the list-endpoint row, see\n`15_get_nfts_list.sql` for the on-the-wire columns), plus a\n`metadata` field fetched at request time via\n`runtime_enrichment::nft_token_uri` — full JSON blob from the\nper-token `token_uri()` IPFS / HTTP URL (attributes, traits,\ndescription, animation_url, etc.). `metadata` is always present\nin the response, set to `null` when the fetch fails or the contract\nreturns a non-JSON `image/*` URI (fail-soft per ADR 0043)."
      },
      "NftItem": {
        "type": "object",
        "description": "One NFT row. Same shape on `GET /v1/nfts` list rows and as the\nflattened core of `GET /v1/nfts/:id` (which adds `metadata`). Pinned\nto canonical SQL `15_get_nfts_list.sql` for the column projection.\n\nThe numeric surrogate `id` was dropped (task 0243 NFT slice): the\nexternal NFT identity is the composite `(contract_id, token_id)` per\ntask 0264 Phase 8a, and ClickHouse — the production datastore — has no\nsurrogate on `nfts` at all (`ORDER BY (contract_id, token_id)`). This\nmirrors the assets `:id`-surrogate drop (PR #175).",
        "required": ["contract_id", "token_id"],
        "properties": {
          "collection_name": {
            "type": ["string", "null"]
          },
          "contract_id": {
            "type": "string",
            "description": "Contract C-StrKey resolved via `soroban_contracts` join."
          },
          "last_seen_ledger": {
            "type": ["integer", "null"],
            "format": "int64",
            "description": "Most recent ledger where ownership state changed\n(`nfts.current_owner_ledger`)."
          },
          "media_url": {
            "type": ["string", "null"]
          },
          "minted_at_ledger": {
            "type": ["integer", "null"],
            "format": "int64"
          },
          "name": {
            "type": ["string", "null"]
          },
          "owner_account": {
            "type": ["string", "null"],
            "description": "Current owner G-StrKey, or `null` for burned NFTs (ADR 0037 §13)."
          },
          "token_id": {
            "type": "string"
          }
        }
      },
      "NftTransferItem": {
        "type": "object",
        "description": "One row of NFT transfer history. Shape pinned to canonical SQL\n`17_get_nfts_transfers.sql`.",
        "required": [
          "transaction_hash",
          "ledger_sequence",
          "event_type",
          "created_at",
          "event_order"
        ],
        "properties": {
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "event_order": {
            "type": "integer",
            "format": "int32"
          },
          "event_type": {
            "type": "integer",
            "format": "int32",
            "description": "Raw NftEventType discriminant (ADR 0031)."
          },
          "event_type_name": {
            "type": ["string", "null"],
            "description": "`mint | transfer | burn` — pre-decoded via `nft_event_type_name(...)`."
          },
          "from_account": {
            "type": ["string", "null"],
            "description": "Previous-owner G-StrKey reconstructed via `LEAD(owner_id)` over the\nper-NFT ownership timeline (DESC window — older event sits at the\nFOLLOWING window position). `null` on the mint row only.\n\nPage boundaries are handled implicitly by the `limit + 1` peek\nfetch: the peek row participates in the window-function input, so\nthe last *kept* row's `from_account` reads the peek's owner before\n`finalize_page` drops the peek. No client-side stitching needed."
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          },
          "to_account": {
            "type": ["string", "null"],
            "description": "New owner G-StrKey. `null` on burn."
          },
          "transaction_hash": {
            "type": "string"
          }
        }
      },
      "OperationItem": {
        "type": "object",
        "required": [
          "appearance_id",
          "type_name",
          "type",
          "pool_ids",
          "ledger_sequence",
          "created_at"
        ],
        "properties": {
          "appearance_id": {
            "type": "integer",
            "format": "int64",
            "description": "Global BIGSERIAL `operations_appearances.id`. Internal ordering\nartefact only; not a within-tx index. Use `application_order`\nfor apply-order display and to join against\n`XdrOperationDto.application_order` from the heavy overlay."
          },
          "application_order": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "1-based per-tx apply position carrying on-chain operation order\n(task 0192). For folded appearance rows (multiple identical-identity\nenvelope ops collapsed into one row, see task 0163) this is the\nMIN of the folded ops' indices — the position of the row's first\noccurrence in `tx.operations[]`. `None` for pre-task-0192 rows\nwhere the column was not yet populated; clients fall back to\n`appearance_id` order in that case."
          },
          "asset_code": {
            "type": ["string", "null"],
            "description": "Asset code (≤12 chars) for classic asset operations."
          },
          "asset_issuer": {
            "type": ["string", "null"]
          },
          "contract_id": {
            "type": ["string", "null"]
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "destination_account": {
            "type": ["string", "null"]
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          },
          "pool_ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Liquidity pools crossed by this operation, as SEP-23 strkeys\n(`L...`, 56 chars). Encoded from the DB hex form at the response\nboundary so cross-entity link targets match the\n`/v1/liquidity-pools/:id` route shape. Single-element for LP\ndeposit/withdraw; the full crossed-pool list for path payments and\noffers that filled against a pool (task 0261/0268 — replaces the\nformer nullable scalar `pool_id`).\n\nEmpty `[]` means \"no pool crossed\" — authoritative on the ClickHouse\nread path, which extracts pool crossings from claim atoms across\npath-payment, offer, and LP deposit/withdraw ops."
          },
          "source_account": {
            "type": ["string", "null"]
          },
          "type": {
            "type": "integer",
            "format": "int32",
            "description": "Raw `OperationType` SMALLINT (ADR 0031)."
          },
          "type_name": {
            "type": "string",
            "description": "Operation type tag in canonical SCREAMING_SNAKE_CASE\n(e.g. `\"INVOKE_HOST_FUNCTION\"`)."
          }
        }
      },
      "OperationType": {
        "type": "string",
        "description": "Stellar operation type. Discriminants match the XDR numbering; the\nserde representation is the canonical SCREAMING_SNAKE_CASE label used\nby the Horizon API and historically persisted as VARCHAR.",
        "enum": [
          "CREATE_ACCOUNT",
          "PAYMENT",
          "PATH_PAYMENT_STRICT_RECEIVE",
          "MANAGE_SELL_OFFER",
          "CREATE_PASSIVE_SELL_OFFER",
          "SET_OPTIONS",
          "CHANGE_TRUST",
          "ALLOW_TRUST",
          "ACCOUNT_MERGE",
          "INFLATION",
          "MANAGE_DATA",
          "BUMP_SEQUENCE",
          "MANAGE_BUY_OFFER",
          "PATH_PAYMENT_STRICT_SEND",
          "CREATE_CLAIMABLE_BALANCE",
          "CLAIM_CLAIMABLE_BALANCE",
          "BEGIN_SPONSORING_FUTURE_RESERVES",
          "END_SPONSORING_FUTURE_RESERVES",
          "REVOKE_SPONSORSHIP",
          "CLAWBACK",
          "CLAWBACK_CLAIMABLE_BALANCE",
          "SET_TRUST_LINE_FLAGS",
          "LIQUIDITY_POOL_DEPOSIT",
          "LIQUIDITY_POOL_WITHDRAW",
          "INVOKE_HOST_FUNCTION",
          "EXTEND_FOOTPRINT_TTL",
          "RESTORE_FOOTPRINT"
        ]
      },
      "PageInfo": {
        "type": "object",
        "description": "Pagination metadata attached to list responses.\n\nCursor-based pagination (not offset) — see ADR 0008. Aligns with\nStellar Horizon conventions and produces stable listings even when\nthe underlying ledger stream advances between requests.\n\nBoth `next_cursor` (forward / next page) and `prev_cursor` (backward\n/ previous page) are **always emitted** as opaque base64-JSON\nstrings or explicit JSON `null`. Wire shape always carries both\nkeys — never omitted — so the OpenAPI `nullable: true` declaration\nmatches the on-wire bytes exactly. `Option<String>` carries the\nentire signal:\n\n- `next_cursor: null` ⇒ last page (no further forward continuation).\n- `next_cursor: \"...\"` ⇒ a further forward page exists.\n- `prev_cursor: null` ⇒ first page (no backward continuation).\n- `prev_cursor: \"...\"` ⇒ a backward page exists.\n\nBoth cursors carry an internal `dir` discriminant (see\n`crates/api/src/common/cursor.rs::Direction`) so the backend can\nbranch SQL between forward (DESC `<`) and backward (ASC `>`,\nreverse for presentation) walks. Clients treat the strings as\nopaque tokens — round-tripping them through `?cursor=` is the only\nexpected use.",
        "required": ["limit"],
        "properties": {
          "limit": {
            "type": "integer",
            "format": "int32",
            "description": "Page size that produced `data`. Echoes the client's requested\nlimit (clamped server-side).",
            "maximum": 100,
            "minimum": 1
          },
          "next_cursor": {
            "type": ["string", "null"],
            "description": "Opaque cursor identifying the next page. `null` when the\nclient has reached the end of the stream."
          },
          "prev_cursor": {
            "type": ["string", "null"],
            "description": "Opaque cursor identifying the previous page. `null` when the\nresponse is the first page (the client has not paged forward\nyet)."
          }
        }
      },
      "Paginated_AccountListItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One row of `GET /v1/accounts`. Identity + native (XLM) balance + the\nfirst/last-seen activity window + `home_domain`. Ordered by\n`last_seen_ledger` (the only indexed sort). `xlm_balance` is the native\nbalance from the unified `balances` table; `null` if no native row exists.",
              "required": [
                "account_id",
                "last_seen_ledger",
                "first_seen_ledger"
              ],
              "properties": {
                "account_id": {
                  "type": "string"
                },
                "first_seen_ledger": {
                  "type": "integer",
                  "format": "int64"
                },
                "home_domain": {
                  "type": ["string", "null"]
                },
                "last_seen_ledger": {
                  "type": "integer",
                  "format": "int64"
                },
                "xlm_balance": {
                  "type": ["string", "null"],
                  "description": "Native (XLM) balance as a RAW `Int128` stroop string (human value =\n÷10⁷); the frontend scales by 7 decimals — same raw-amount contract as\nthe account-detail balances."
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_AccountTransactionItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "Slim — `inner_tx_hash` lives on `/v1/transactions` only.",
              "required": [
                "hash",
                "ledger_sequence",
                "application_order",
                "source_account",
                "fee_charged",
                "successful",
                "operation_count",
                "has_soroban",
                "operation_types",
                "values",
                "created_at"
              ],
              "properties": {
                "application_order": {
                  "type": "integer",
                  "format": "int32",
                  "description": "1-based position in ledger."
                },
                "created_at": {
                  "type": "string",
                  "format": "date-time"
                },
                "fee_charged": {
                  "type": "integer",
                  "format": "int64",
                  "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
                },
                "has_soroban": {
                  "type": "boolean"
                },
                "hash": {
                  "type": "string",
                  "description": "64-char lowercase hex."
                },
                "ledger_sequence": {
                  "type": "integer",
                  "format": "int64"
                },
                "operation_count": {
                  "type": "integer",
                  "format": "int32"
                },
                "operation_types": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "source_account": {
                  "type": "string"
                },
                "successful": {
                  "type": "boolean"
                },
                "values": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TransactionValue"
                  },
                  "description": "Net-settled \"value moved\" per asset the transaction touched (task 0393);\nsee `TransactionValue`. Raw amounts + `decimals` — the frontend scales."
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_AssetItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "Asset row returned by list and detail. Surfaces both the decoded\n`asset_type_name` (SQL `token_asset_type_name()`) and the raw `asset_type`\nSMALLINT — canonical SQL `08_get_assets_list.sql` projection.",
              "required": ["id", "asset_type", "decimals"],
              "properties": {
                "asset_code": {
                  "type": ["string", "null"]
                },
                "asset_type": {
                  "type": "integer",
                  "format": "int32",
                  "description": "Raw SMALLINT (0=native, 1=classic_credit, 3=soroban). 2 (`sac`) is retired."
                },
                "asset_type_name": {
                  "type": ["string", "null"],
                  "description": "`native | classic_credit | soroban` (ADR 0051 — `sac` retired). `null`\nonly on schema drift."
                },
                "contract_id": {
                  "type": ["string", "null"],
                  "description": "Soroban contract StrKey (`C…`) — set ONLY for `soroban` (type=3), where\nthe contract IS the asset. `null` for native / classic_credit (a wrapping\nSAC's address rides in `sac_contract_id`)."
                },
                "decimals": {
                  "type": "integer",
                  "format": "int32",
                  "description": "Display decimals — on-chain `METADATA` for Soroban tokens, else 7\n(Stellar classic precision). Load-bearing for amount rendering.",
                  "minimum": 0
                },
                "holder_count": {
                  "type": ["integer", "null"],
                  "format": "int32",
                  "description": "Active-holder count (`amount > 0`) from the unified `balances` aggregate\n(all asset types — trustline holders + contract holders). `null` = no data."
                },
                "icon_url": {
                  "type": ["string", "null"]
                },
                "id": {
                  "type": "string",
                  "description": "Canonical identifier — the single token usable as `/assets/{id}`:\nthe reserved `native` token for native XLM, the contract StrKey\n(`C…`) for contract-backed assets (SAC / Soroban), otherwise the\n`CODE-ISSUER` composite (classic credit, e.g. `USDC-GA…`). Replaces\nthe dropped numeric surrogate (PR #175): assets are keyed on\n`(asset_type, asset_code, issuer_id, contract_id)`, with no surrogate."
                },
                "issuer": {
                  "type": ["string", "null"]
                },
                "issuer_home_domain": {
                  "type": ["string", "null"],
                  "description": "The issuer account's on-chain `home_domain` (SEP-1 anchor domain), e.g.\n`centre.io`. Read from the same `accounts` key-seek that resolves\n`issuer`, so it costs no extra query (task 0450). `null` for native /\nSoroban-native assets (no issuer) and for issuers that never set one —\nmost do not.\n\nNOT a verified identity: the account holder sets this field itself and\nnothing checks it. Render it as a claim, never as a badge implying we\nconfirmed the domain owns the asset.\n\nDistinct from the detail response's `home_page`, which is fetched from\nthe issuer's `stellar.toml` at request time."
                },
                "name": {
                  "type": ["string", "null"]
                },
                "sac_contract_id": {
                  "type": ["string", "null"],
                  "description": "SAC facet (ADR 0051): the wrapping Stellar Asset Contract's `C…` StrKey\nfor a native / classic_credit asset that has one, re-derived on read from\n`code:issuer` (never stored). `null` when the asset has no observed SAC."
                },
                "sac_deployed": {
                  "type": ["boolean", "null"],
                  "description": "Whether `sac_contract_id`'s SAC is deployed on-chain (ADR 0051). `null`\nwhen the asset has no SAC; `false` = reserved-but-un-deployed address\n(render the contract link non-clickable)."
                },
                "symbol": {
                  "type": ["string", "null"],
                  "description": "On-chain SEP-41 token symbol (Soroban `METADATA`). `null` for classic\n(use `asset_code`) and native."
                },
                "total_supply": {
                  "type": ["string", "null"],
                  "description": "Total supply as a RAW integer string (`Int128`) — scale by `decimals` for\ndisplay (task 0331 Option C: one convention for ALL asset types; classic\n`decimals` is 7). E.g. `\"63836094715548\"`. `null` = no balance data\n(a token/asset with no holders). Sourced from `balance_aggregates` over the\nunified `balances` table."
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_AssetTransactionItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "Transaction row for `/assets/:id/transactions`. Pure-DB; mirrors\ncanonical SQL `10_get_assets_transactions.sql`.",
              "required": [
                "hash",
                "ledger_sequence",
                "source_account",
                "successful",
                "fee_charged",
                "created_at",
                "operation_count",
                "has_soroban",
                "operation_types"
              ],
              "properties": {
                "created_at": {
                  "type": "string",
                  "format": "date-time"
                },
                "fee_charged": {
                  "type": "integer",
                  "format": "int64",
                  "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
                },
                "has_soroban": {
                  "type": "boolean"
                },
                "hash": {
                  "type": "string"
                },
                "ledger_sequence": {
                  "type": "integer",
                  "format": "int64"
                },
                "operation_count": {
                  "type": "integer",
                  "format": "int32"
                },
                "operation_types": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Distinct `op_type_name(...)` labels for every op in the tx, sorted asc."
                },
                "source_account": {
                  "type": "string"
                },
                "successful": {
                  "type": "boolean"
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_ContractListItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One row of `GET /v1/contracts`. Identity + classification + deploy\nprovenance + a 7-day activity signal. All fields come straight from\n`soroban_contracts` (+ a deployer join and a windowed invocation count);\nnullable fields are `None` until the contract's deploy is observed.",
              "required": ["contract_id", "is_sac", "recent_invocations"],
              "properties": {
                "contract_id": {
                  "type": "string"
                },
                "contract_type": {
                  "type": ["integer", "null"],
                  "format": "int32",
                  "description": "Raw SMALLINT class (0=token, 1=other, 2=nft, 3=fungible). `null`\nuntil deployment is observed."
                },
                "contract_type_name": {
                  "type": ["string", "null"],
                  "description": "`token | other | nft | fungible`. `null` only on schema drift / no type."
                },
                "deployed_at_ledger": {
                  "type": ["integer", "null"],
                  "format": "int64",
                  "description": "Ledger the deploy was observed at; `null` until then."
                },
                "deployer": {
                  "type": ["string", "null"],
                  "description": "Deployer account G-strkey; `null` until the deploy op is observed."
                },
                "is_sac": {
                  "type": "boolean",
                  "description": "Stellar Asset Contract flag (stored, not derived from `contract_type`)."
                },
                "recent_invocations": {
                  "type": "integer",
                  "format": "int64",
                  "description": "Invocation count over the last 7 days (windowed; matches the detail\n`ContractStats.recent_invocations` semantics)."
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_EventItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One row per event — the full-content `soroban_events` table stores one\nrow per event (no appearance-fold expansion).",
              "required": [
                "transaction_hash",
                "ledger_sequence",
                "transaction_id",
                "successful",
                "created_at",
                "event_type",
                "topics",
                "data"
              ],
              "properties": {
                "created_at": {
                  "type": "string",
                  "format": "date-time"
                },
                "data": {},
                "event_type": {
                  "type": "string"
                },
                "ledger_sequence": {
                  "type": "integer",
                  "format": "int64"
                },
                "successful": {
                  "type": "boolean"
                },
                "topics": {
                  "type": "array",
                  "items": {}
                },
                "transaction_hash": {
                  "type": "string"
                },
                "transaction_id": {
                  "type": "integer",
                  "format": "int64"
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_InvocationItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "transaction_hash",
                "ledger_sequence",
                "created_at",
                "successful"
              ],
              "properties": {
                "caller_account": {
                  "type": ["string", "null"]
                },
                "created_at": {
                  "type": "string",
                  "format": "date-time"
                },
                "ledger_sequence": {
                  "type": "integer",
                  "format": "int64"
                },
                "successful": {
                  "type": "boolean"
                },
                "transaction_hash": {
                  "type": "string"
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_LedgerListItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "Slim ledger row returned in the list endpoint and reused inside the\ndetail response as the header block.",
              "required": [
                "sequence",
                "hash",
                "closed_at",
                "protocol_version",
                "transaction_count",
                "base_fee"
              ],
              "properties": {
                "base_fee": {
                  "type": "integer",
                  "format": "int64"
                },
                "closed_at": {
                  "type": "string",
                  "format": "date-time"
                },
                "hash": {
                  "type": "string",
                  "description": "Ledger hash (64-char lowercase hex)."
                },
                "protocol_version": {
                  "type": "integer",
                  "format": "int32"
                },
                "sequence": {
                  "type": "integer",
                  "format": "int64"
                },
                "transaction_count": {
                  "type": "integer",
                  "format": "int32"
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_NftItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One NFT row. Same shape on `GET /v1/nfts` list rows and as the\nflattened core of `GET /v1/nfts/:id` (which adds `metadata`). Pinned\nto canonical SQL `15_get_nfts_list.sql` for the column projection.\n\nThe numeric surrogate `id` was dropped (task 0243 NFT slice): the\nexternal NFT identity is the composite `(contract_id, token_id)` per\ntask 0264 Phase 8a, and ClickHouse — the production datastore — has no\nsurrogate on `nfts` at all (`ORDER BY (contract_id, token_id)`). This\nmirrors the assets `:id`-surrogate drop (PR #175).",
              "required": ["contract_id", "token_id"],
              "properties": {
                "collection_name": {
                  "type": ["string", "null"]
                },
                "contract_id": {
                  "type": "string",
                  "description": "Contract C-StrKey resolved via `soroban_contracts` join."
                },
                "last_seen_ledger": {
                  "type": ["integer", "null"],
                  "format": "int64",
                  "description": "Most recent ledger where ownership state changed\n(`nfts.current_owner_ledger`)."
                },
                "media_url": {
                  "type": ["string", "null"]
                },
                "minted_at_ledger": {
                  "type": ["integer", "null"],
                  "format": "int64"
                },
                "name": {
                  "type": ["string", "null"]
                },
                "owner_account": {
                  "type": ["string", "null"],
                  "description": "Current owner G-StrKey, or `null` for burned NFTs (ADR 0037 §13)."
                },
                "token_id": {
                  "type": "string"
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_NftTransferItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One row of NFT transfer history. Shape pinned to canonical SQL\n`17_get_nfts_transfers.sql`.",
              "required": [
                "transaction_hash",
                "ledger_sequence",
                "event_type",
                "created_at",
                "event_order"
              ],
              "properties": {
                "created_at": {
                  "type": "string",
                  "format": "date-time"
                },
                "event_order": {
                  "type": "integer",
                  "format": "int32"
                },
                "event_type": {
                  "type": "integer",
                  "format": "int32",
                  "description": "Raw NftEventType discriminant (ADR 0031)."
                },
                "event_type_name": {
                  "type": ["string", "null"],
                  "description": "`mint | transfer | burn` — pre-decoded via `nft_event_type_name(...)`."
                },
                "from_account": {
                  "type": ["string", "null"],
                  "description": "Previous-owner G-StrKey reconstructed via `LEAD(owner_id)` over the\nper-NFT ownership timeline (DESC window — older event sits at the\nFOLLOWING window position). `null` on the mint row only.\n\nPage boundaries are handled implicitly by the `limit + 1` peek\nfetch: the peek row participates in the window-function input, so\nthe last *kept* row's `from_account` reads the peek's owner before\n`finalize_page` drops the peek. No client-side stitching needed."
                },
                "ledger_sequence": {
                  "type": "integer",
                  "format": "int64"
                },
                "to_account": {
                  "type": ["string", "null"],
                  "description": "New owner G-StrKey. `null` on burn."
                },
                "transaction_hash": {
                  "type": "string"
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_ParticipantItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One participant row returned by the participants list. Shape pinned to\n`docs/architecture/database-schema/endpoint-queries-clickhouse/23_get_liquidity_pools_participants.sql`.",
              "required": [
                "account",
                "shares",
                "first_deposit_ledger",
                "last_updated_ledger"
              ],
              "properties": {
                "account": {
                  "type": "string",
                  "description": "Participant account StrKey (G...)."
                },
                "first_deposit_ledger": {
                  "type": "integer",
                  "format": "int64",
                  "description": "Ledger of the first deposit by this account into this pool."
                },
                "last_updated_ledger": {
                  "type": "integer",
                  "format": "int64",
                  "description": "Ledger of the most recent change to this position."
                },
                "share_percentage": {
                  "type": ["string", "null"],
                  "description": "Share of the pool, expressed as a decimal-string percentage\n(`100 * shares / total_pool_shares`). `None` when the pool has no\nsnapshot in the freshness window (stale pool); the frontend renders\nit as \"—\" in that case (matches the list-endpoint stale-pool\nconvention from `18_get_liquidity_pools_list.sql`)."
                },
                "shares": {
                  "type": "string",
                  "description": "Pool-share balance carried as a decimal string preserving the\nunderlying `NUMERIC(28,7)` precision (no f64 round-trip)."
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_PoolItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One pool row returned by the list endpoint. Shape pinned to canonical\nSQL `18_get_liquidity_pools_list.sql`. Pools without a fresh snapshot\nin the freshness window come back with `null` for every dynamic field\n(`reserve_a`, `reserve_b`, `total_shares`, `tvl`, `volume`,\n`fee_revenue`, `latest_snapshot_*`); frontend renders these as \"stale\".",
              "required": [
                "pool_id",
                "asset_a",
                "asset_b",
                "fee_bps",
                "fee_percent",
                "created_at_ledger",
                "participant_count"
              ],
              "properties": {
                "asset_a": {
                  "$ref": "#/components/schemas/PoolAssetLeg"
                },
                "asset_b": {
                  "$ref": "#/components/schemas/PoolAssetLeg"
                },
                "created_at_ledger": {
                  "type": "integer",
                  "format": "int64"
                },
                "fee_bps": {
                  "type": "integer",
                  "format": "int32"
                },
                "fee_percent": {
                  "type": "string",
                  "description": "`fee_bps / 100` as decimal string. Conversion done server-side so\nthe frontend can render directly (frontend §6.13/§6.14)."
                },
                "fee_revenue": {
                  "type": ["string", "null"]
                },
                "latest_snapshot_at": {
                  "type": ["string", "null"],
                  "format": "date-time"
                },
                "latest_snapshot_ledger": {
                  "type": ["integer", "null"],
                  "format": "int64"
                },
                "participant_count": {
                  "type": "integer",
                  "format": "int64",
                  "description": "Count of active liquidity providers (`lp_positions WHERE shares > 0`).\nComputed from the live table — not dependent on the snapshot\nfreshness window, so it is populated even on stale pools (where\n`tvl`/`volume`/`fee_revenue` are NULL)."
                },
                "pool_id": {
                  "type": "string",
                  "description": "SEP-23 strkey (`L...`, 56 chars). DB stores `BYTEA(32)` per ADR\n0024; the handler encodes to strkey at the response boundary so\nthe wire shape matches the Stellar ecosystem canonical form\n(CAP-38 / SEP-23)."
                },
                "reserve_a": {
                  "type": ["string", "null"]
                },
                "reserve_b": {
                  "type": ["string", "null"]
                },
                "total_shares": {
                  "type": ["string", "null"]
                },
                "tvl": {
                  "type": ["string", "null"]
                },
                "volume": {
                  "type": ["string", "null"]
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_PoolTransactionItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One row from `/liquidity-pools/:id/transactions`. Shape pinned to\ncanonical SQL `20_get_liquidity_pools_transactions.sql`.",
              "required": [
                "hash",
                "ledger_sequence",
                "source_account",
                "fee_charged",
                "successful",
                "operation_count",
                "has_soroban",
                "operation_types",
                "created_at"
              ],
              "properties": {
                "created_at": {
                  "type": "string",
                  "format": "date-time"
                },
                "fee_charged": {
                  "type": "integer",
                  "format": "int64",
                  "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
                },
                "has_soroban": {
                  "type": "boolean"
                },
                "hash": {
                  "type": "string"
                },
                "ledger_sequence": {
                  "type": "integer",
                  "format": "int64"
                },
                "operation_count": {
                  "type": "integer",
                  "format": "int32"
                },
                "operation_types": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Distinct `op_type_name(...)` labels for every op in the tx, sorted\nasc. Frontend §6.14 categorises trade vs LP-mgmt activity from this\nlist (policy lives client-side, not in SQL)."
                },
                "source_account": {
                  "type": "string"
                },
                "successful": {
                  "type": "boolean"
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Paginated_TransactionListItem": {
        "type": "object",
        "description": "Canonical envelope for paginated list responses.\n\nGeneric over the item type `T` so every endpoint can reuse a single\nshape. Concrete instantiations (e.g. `Paginated<Transaction>`) are\npicked up automatically by utoipa-axum via the handler return type\nwhen M2 endpoint modules are wired in. Unused in M1 — kept as\ninfrastructure that M2 endpoints will consume.",
        "required": ["data", "page"],
        "properties": {
          "data": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "Slim transaction row returned in the list endpoint.",
              "required": [
                "hash",
                "ledger_sequence",
                "application_order",
                "fee_charged",
                "successful",
                "operation_count",
                "has_soroban",
                "operation_types",
                "values",
                "created_at"
              ],
              "properties": {
                "application_order": {
                  "type": "integer",
                  "format": "int32",
                  "description": "1-based position of this transaction within its ledger."
                },
                "created_at": {
                  "type": "string",
                  "format": "date-time"
                },
                "fee_charged": {
                  "type": "integer",
                  "format": "int64",
                  "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
                },
                "has_soroban": {
                  "type": "boolean",
                  "description": "`true` when the transaction touched at least one Soroban contract\n(root invocation, nested call, or event emission)."
                },
                "hash": {
                  "type": "string",
                  "description": "Transaction hash (64-char lowercase hex)."
                },
                "inner_tx_hash": {
                  "type": ["string", "null"],
                  "description": "Inner-transaction hash (64-char hex) for fee-bump envelopes, `null` otherwise."
                },
                "ledger_sequence": {
                  "type": "integer",
                  "format": "int64"
                },
                "operation_count": {
                  "type": "integer",
                  "format": "int32"
                },
                "operation_types": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "All distinct operation type names in the transaction\n(e.g. `[\"INVOKE_HOST_FUNCTION\", \"PAYMENT\"]`)."
                },
                "source_account": {
                  "type": ["string", "null"],
                  "description": "`null` for Variant A `parse_error` transactions whose envelope\ncould not be decoded (lore-0209). Always populated for ordinary\n(successful or failed-but-decoded) transactions."
                },
                "successful": {
                  "type": "boolean"
                },
                "values": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TransactionValue"
                  },
                  "description": "Net-settled \"value moved\" per asset the transaction touched (task 0393):\n`max(Σ+, Σ−)` per (transaction, asset). Raw values + `decimals` — the\nfrontend scales. Ordered native-first (`asset_type`, then `asset_id`) so\n`values[0]` is XLM when the tx moved it; empty when nothing net-settled\n(a wash / pure cycle is zero by the flow decomposition theorem) or when\nhistory has not been backfilled yet."
                }
              }
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "ParticipantItem": {
        "type": "object",
        "description": "One participant row returned by the participants list. Shape pinned to\n`docs/architecture/database-schema/endpoint-queries-clickhouse/23_get_liquidity_pools_participants.sql`.",
        "required": [
          "account",
          "shares",
          "first_deposit_ledger",
          "last_updated_ledger"
        ],
        "properties": {
          "account": {
            "type": "string",
            "description": "Participant account StrKey (G...)."
          },
          "first_deposit_ledger": {
            "type": "integer",
            "format": "int64",
            "description": "Ledger of the first deposit by this account into this pool."
          },
          "last_updated_ledger": {
            "type": "integer",
            "format": "int64",
            "description": "Ledger of the most recent change to this position."
          },
          "share_percentage": {
            "type": ["string", "null"],
            "description": "Share of the pool, expressed as a decimal-string percentage\n(`100 * shares / total_pool_shares`). `None` when the pool has no\nsnapshot in the freshness window (stale pool); the frontend renders\nit as \"—\" in that case (matches the list-endpoint stale-pool\nconvention from `18_get_liquidity_pools_list.sql`)."
          },
          "shares": {
            "type": "string",
            "description": "Pool-share balance carried as a decimal string preserving the\nunderlying `NUMERIC(28,7)` precision (no f64 round-trip)."
          }
        }
      },
      "PoolAssetLeg": {
        "type": "object",
        "description": "One leg of an LP's asset pair. Surfaces both the decoded\n`asset_type_name` (SQL `asset_type_name()`) and the raw `asset_type`\nSMALLINT — same contract as `assets/dto::AssetItem`.\n\nLinkable identifiers (task 0263 / F-K-9). All link targets are the\n**asset detail page** (`/assets/...`) — `parse_asset_id` is polymorphic\nand accepts both C-strkey (SAC) and `code-issuer` composite, so all\nnon-native legs resolve to the same asset row.\n\n  * `asset_type == 0` — native XLM; FE renders unlinked (no on-chain\n    address in classic Stellar protocol; SAC mirror is network-dependent).\n  * `contract_id` — C-strkey of the SAC mirror for a classic credit\n    leg (populated when the leg's `(asset_code, issuer)` classic_credit /\n    native `assets` row carries a deployed SAC facet — `sac_contract_id`\n    resolving a `soroban_contracts.contract_id`, ADR 0051). `None` for legs\n    without a deployed SAC mirror. Pool legs only carry XDR `AssetType` (native /\n    credit_alphanum4 / credit_alphanum12) per `0006_liquidity_pools.sql`,\n    so SAC / Soroban legs are not directly representable here;\n    `contract_id` surfaces the SAC mirror look-up so the FE can\n    route to the asset detail page via `/assets/${contract_id}`.\n  * `issuer` + `asset_code` (classic credit, no SAC mirror) — FE\n    routes to `/assets/${asset_code}-${issuer}` (composite form\n    accepted by `parse_asset_id`).",
        "required": ["asset_type"],
        "properties": {
          "asset_code": {
            "type": ["string", "null"]
          },
          "asset_type": {
            "type": "integer",
            "format": "int32",
            "description": "Raw SMALLINT (0=native, 1=classic_credit, 3=soroban). 2 (`sac`) is retired."
          },
          "asset_type_name": {
            "type": ["string", "null"],
            "description": "`native | classic_credit | soroban` (ADR 0051 — `sac` retired). `null`\nonly on schema drift."
          },
          "contract_id": {
            "type": ["string", "null"],
            "description": "C-strkey of the deployed SAC mirror for the leg's `(asset_code, issuer)`\nclassic_credit / native asset (ADR 0051 — resolved via the row's\n`sac_contract_id` facet). `None` for native legs and for classic credit\nlegs without a deployed SAC."
          },
          "icon_url": {
            "type": ["string", "null"],
            "description": "Asset icon URL, mirrored from the leg's `assets.icon_url` row so\npool avatars render the same icon as the assets list. `None` for\nnative legs and assets without an enriched icon — the FE falls back\nto the asset-code initial."
          },
          "issuer": {
            "type": ["string", "null"]
          }
        }
      },
      "PoolItem": {
        "type": "object",
        "description": "One pool row returned by the list endpoint. Shape pinned to canonical\nSQL `18_get_liquidity_pools_list.sql`. Pools without a fresh snapshot\nin the freshness window come back with `null` for every dynamic field\n(`reserve_a`, `reserve_b`, `total_shares`, `tvl`, `volume`,\n`fee_revenue`, `latest_snapshot_*`); frontend renders these as \"stale\".",
        "required": [
          "pool_id",
          "asset_a",
          "asset_b",
          "fee_bps",
          "fee_percent",
          "created_at_ledger",
          "participant_count"
        ],
        "properties": {
          "asset_a": {
            "$ref": "#/components/schemas/PoolAssetLeg"
          },
          "asset_b": {
            "$ref": "#/components/schemas/PoolAssetLeg"
          },
          "created_at_ledger": {
            "type": "integer",
            "format": "int64"
          },
          "fee_bps": {
            "type": "integer",
            "format": "int32"
          },
          "fee_percent": {
            "type": "string",
            "description": "`fee_bps / 100` as decimal string. Conversion done server-side so\nthe frontend can render directly (frontend §6.13/§6.14)."
          },
          "fee_revenue": {
            "type": ["string", "null"]
          },
          "latest_snapshot_at": {
            "type": ["string", "null"],
            "format": "date-time"
          },
          "latest_snapshot_ledger": {
            "type": ["integer", "null"],
            "format": "int64"
          },
          "participant_count": {
            "type": "integer",
            "format": "int64",
            "description": "Count of active liquidity providers (`lp_positions WHERE shares > 0`).\nComputed from the live table — not dependent on the snapshot\nfreshness window, so it is populated even on stale pools (where\n`tvl`/`volume`/`fee_revenue` are NULL)."
          },
          "pool_id": {
            "type": "string",
            "description": "SEP-23 strkey (`L...`, 56 chars). DB stores `BYTEA(32)` per ADR\n0024; the handler encodes to strkey at the response boundary so\nthe wire shape matches the Stellar ecosystem canonical form\n(CAP-38 / SEP-23)."
          },
          "reserve_a": {
            "type": ["string", "null"]
          },
          "reserve_b": {
            "type": ["string", "null"]
          },
          "total_shares": {
            "type": ["string", "null"]
          },
          "tvl": {
            "type": ["string", "null"]
          },
          "volume": {
            "type": ["string", "null"]
          }
        }
      },
      "PoolTransactionItem": {
        "type": "object",
        "description": "One row from `/liquidity-pools/:id/transactions`. Shape pinned to\ncanonical SQL `20_get_liquidity_pools_transactions.sql`.",
        "required": [
          "hash",
          "ledger_sequence",
          "source_account",
          "fee_charged",
          "successful",
          "operation_count",
          "has_soroban",
          "operation_types",
          "created_at"
        ],
        "properties": {
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "fee_charged": {
            "type": "integer",
            "format": "int64",
            "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
          },
          "has_soroban": {
            "type": "boolean"
          },
          "hash": {
            "type": "string"
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          },
          "operation_count": {
            "type": "integer",
            "format": "int32"
          },
          "operation_types": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Distinct `op_type_name(...)` labels for every op in the tx, sorted\nasc. Frontend §6.14 categorises trade vs LP-mgmt activity from this\nlist (policy lives client-side, not in SQL)."
          },
          "source_account": {
            "type": "string"
          },
          "successful": {
            "type": "boolean"
          }
        }
      },
      "SearchGroups": {
        "type": "object",
        "description": "Per-entity buckets. Empty buckets are omitted from the JSON output\nvia `skip_serializing_if` — frontend treats absent and empty array\nidentically, and dropping empties keeps the dropdown payload tight\nwhen a typed `?type=` filter narrows results to a single bucket.",
        "properties": {
          "accounts": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SearchHit"
            }
          },
          "assets": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SearchHit"
            }
          },
          "contracts": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SearchHit"
            }
          },
          "nfts": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SearchHit"
            }
          },
          "pools": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SearchHit"
            }
          },
          "transactions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SearchHit"
            }
          }
        }
      },
      "SearchHit": {
        "type": "object",
        "description": "Single search row. Same shape across every entity bucket.\n\n`identifier` is the canonical human-shown id (hex hash for\ntransactions, strkey `L…` for pools, StrKey for accounts /\ncontracts, asset code for assets, name for NFTs). It is the routing\nkey too for every type whose display id IS routable (transaction,\naccount, contract, pool). The two exceptions carry a separate routing\npayload because their display id is NOT routable:\n\n- `asset`: `identifier` is the asset code (not unique / not routable);\n  `route_token` carries the canonical `/assets/:id` token (contract\n  StrKey | `CODE-ISSUER` | `native`), mirroring `canonical_id` in\n  `assets/handlers.rs`.\n- `nft`: identity is the composite `(contract_id, token_id)` per task\n  0264 / ADR 0030 — the two fields below carry it for routing.\n\n`successful` and `last_activity_at` are populated only for\n`entity_type = transaction` today — joined from the partitioned\n`transactions` table via `(hash, created_at)` for partition pruning.\nOther entity types pass `None` for both until follow-up enrichment\nadds per-entity last-activity joins. The frontend renders a\nstatus chip + relative timestamp on the right side of the row\nwhenever these fields are present.",
        "required": ["entity_type", "identifier", "label"],
        "properties": {
          "contract_id": {
            "type": ["string", "null"],
            "description": "NFT composite routing key. Populated only when\n`entity_type = nft`; `None` for every other bucket."
          },
          "entity_type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "identifier": {
            "type": "string"
          },
          "label": {
            "type": "string"
          },
          "last_activity_at": {
            "type": ["string", "null"],
            "format": "date-time"
          },
          "route_token": {
            "type": ["string", "null"],
            "description": "Canonical `/assets/:id` routing token for `asset` hits; `None` for\nevery other type (they route on `identifier`). See the struct doc."
          },
          "successful": {
            "type": ["boolean", "null"]
          },
          "token_id": {
            "type": ["string", "null"],
            "description": "NFT composite routing key. Populated only when\n`entity_type = nft`; `None` for every other bucket."
          }
        }
      },
      "SearchResults": {
        "type": "object",
        "description": "Results payload — six entity-typed buckets, each capped at the\nper-group `limit` chosen by the caller (default 10, ceiling 50).\n\nFE decides \"singleton → navigate directly\" by inspecting\n`groups`: when the total row count across all buckets is exactly\n1 and `routeForHit(singleton)` resolves, the FE navigates to the\ndetail page; otherwise it renders the dropdown / Results page.",
        "required": ["groups"],
        "properties": {
          "groups": {
            "$ref": "#/components/schemas/SearchGroups"
          }
        }
      },
      "SignatureDto": {
        "type": "object",
        "description": "Single signature on a transaction envelope.",
        "required": ["hint", "signature"],
        "properties": {
          "hint": {
            "type": "string",
            "description": "4-byte hint (lowercase hex, 8 chars)."
          },
          "signature": {
            "type": "string",
            "description": "Signature bytes (lowercase hex)."
          }
        }
      },
      "TransactionDetailLight": {
        "type": "object",
        "description": "DB-sourced light slice for the transaction detail endpoint.\n\nComposed with `E3HeavyFields` via `merge_e3_response` (task 0150). All\nXDR-sourced fields (memo, result_code, signatures, events, operation\ndetails, envelope_xdr/result_xdr, operation_tree) live in `heavy`.",
        "required": [
          "hash",
          "ledger_sequence",
          "application_order",
          "fee_charged",
          "successful",
          "operation_count",
          "has_soroban",
          "created_at",
          "parse_error",
          "operations",
          "participants",
          "soroban_events",
          "soroban_invocations"
        ],
        "properties": {
          "application_order": {
            "type": "integer",
            "format": "int32",
            "description": "1-based position of this transaction within its ledger."
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "fee_charged": {
            "type": "integer",
            "format": "int64",
            "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
          },
          "has_soroban": {
            "type": "boolean"
          },
          "hash": {
            "type": "string",
            "description": "Transaction hash (64-char lowercase hex)."
          },
          "inner_tx_hash": {
            "type": ["string", "null"],
            "description": "Inner-transaction hash (64-char hex) for fee-bump envelopes, `null` otherwise."
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          },
          "operation_count": {
            "type": "integer",
            "format": "int32"
          },
          "operations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OperationItem"
            }
          },
          "parse_error": {
            "type": "boolean",
            "description": "`true` when the XDR parser encountered an error for this transaction."
          },
          "participants": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Accounts touched by this transaction. Populated only when\n`heavy_fields_status = \"unavailable\"`; otherwise `[]` and consumers\nshould rely on the heavy block."
          },
          "soroban_events": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EventAppearanceItem"
            },
            "description": "Soroban event appearance index rows. Same fallback semantics as\n`participants`. Full topics + data live in `heavy.contract_events`."
          },
          "soroban_invocations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/InvocationAppearanceItem"
            },
            "description": "Soroban invocation appearance index rows. Same fallback semantics\nas `participants`. Full call hierarchy lives in `heavy.operation_tree`."
          },
          "source_account": {
            "type": ["string", "null"],
            "description": "`null` for Variant A `parse_error` transactions whose envelope\ncould not be decoded (lore-0209)."
          },
          "successful": {
            "type": "boolean"
          }
        }
      },
      "TransactionListItem": {
        "type": "object",
        "description": "Slim transaction row returned in the list endpoint.",
        "required": [
          "hash",
          "ledger_sequence",
          "application_order",
          "fee_charged",
          "successful",
          "operation_count",
          "has_soroban",
          "operation_types",
          "values",
          "created_at"
        ],
        "properties": {
          "application_order": {
            "type": "integer",
            "format": "int32",
            "description": "1-based position of this transaction within its ledger."
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "fee_charged": {
            "type": "integer",
            "format": "int64",
            "description": "Fee charged, in raw stroops. Native (XLM) is always 7 decimals, so\nthere is no `decimals` field — the frontend scales by 1e7."
          },
          "has_soroban": {
            "type": "boolean",
            "description": "`true` when the transaction touched at least one Soroban contract\n(root invocation, nested call, or event emission)."
          },
          "hash": {
            "type": "string",
            "description": "Transaction hash (64-char lowercase hex)."
          },
          "inner_tx_hash": {
            "type": ["string", "null"],
            "description": "Inner-transaction hash (64-char hex) for fee-bump envelopes, `null` otherwise."
          },
          "ledger_sequence": {
            "type": "integer",
            "format": "int64"
          },
          "operation_count": {
            "type": "integer",
            "format": "int32"
          },
          "operation_types": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "All distinct operation type names in the transaction\n(e.g. `[\"INVOKE_HOST_FUNCTION\", \"PAYMENT\"]`)."
          },
          "source_account": {
            "type": ["string", "null"],
            "description": "`null` for Variant A `parse_error` transactions whose envelope\ncould not be decoded (lore-0209). Always populated for ordinary\n(successful or failed-but-decoded) transactions."
          },
          "successful": {
            "type": "boolean"
          },
          "values": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TransactionValue"
            },
            "description": "Net-settled \"value moved\" per asset the transaction touched (task 0393):\n`max(Σ+, Σ−)` per (transaction, asset). Raw values + `decimals` — the\nfrontend scales. Ordered native-first (`asset_type`, then `asset_id`) so\n`values[0]` is XLM when the tx moved it; empty when nothing net-settled\n(a wash / pure cycle is zero by the flow decomposition theorem) or when\nhistory has not been backfilled yet."
          }
        }
      },
      "TransactionValue": {
        "type": "object",
        "description": "One asset's net-settled \"value moved\" in a transaction (task 0393).",
        "required": ["asset", "net_settled", "decimals"],
        "properties": {
          "asset": {
            "type": "string",
            "description": "Asset identity for the asset detail link — `\"native\"`, `\"CODE-ISSUER\"`, or a\ncontract `C…` StrKey for a bespoke type-3 Soroban token (all accepted by\n`GET /assets/{id}`)."
          },
          "asset_code": {
            "type": ["string", "null"],
            "description": "Asset code for display (e.g. `\"USDC\"`) — the on-chain token symbol for a\nbespoke type-3 token; `null` for native XLM and for a bespoke token whose\nmetadata symbol is unavailable."
          },
          "decimals": {
            "type": "integer",
            "format": "int32",
            "description": "Display decimals (`7` for classic / SAC assets).",
            "minimum": 0
          },
          "net_settled": {
            "type": "string",
            "description": "Raw net-settled value as a stringified `Int128`; scale by `decimals`."
          }
        }
      },
      "XdrEventDto": {
        "type": "object",
        "description": "Common shape for contract events and diagnostic events.",
        "required": ["event_type", "topics", "data", "event_index"],
        "properties": {
          "contract_id": {
            "type": ["string", "null"],
            "description": "Contract address (StrKey) that emitted the event, if any."
          },
          "data": {
            "description": "Decoded event data payload."
          },
          "event_index": {
            "type": "integer",
            "format": "int32",
            "description": "Event index within the transaction (zero-based)."
          },
          "event_type": {
            "type": "string",
            "description": "`\"contract\"`, `\"system\"`, or `\"diagnostic\"`."
          },
          "op_index": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "Zero-based envelope position of the operation that emitted this event\n(CAP-67 V4 per-operation container only; `None` for tx-level,\ndiagnostic and pre-Protocol-23 events). Matches\n`XdrOperationDto.application_order - 1`."
          },
          "stage": {
            "type": ["string", "null"],
            "description": "CAP-67 `TransactionEvent.stage` — `\"before_all_txs\"`, `\"after_tx\"` or\n`\"after_all_txs\"`. The protocol's only statement of when a tx-level\nevent fired, and the reason `event_index` must not be read as a\ntimeline: the fee refund is numbered ahead of the operation it\nrefunds. Observed values on mainnet are `before_all_txs` for the charge\nand `after_all_txs` for the refund; `after_tx` exists in the protocol\nand is passed through unchanged if it appears. `None` for per-operation, diagnostic and\npre-Protocol-23 events, which carry no stage."
          },
          "topics": {
            "type": "array",
            "items": {},
            "description": "Decoded topic array."
          }
        }
      },
      "XdrOperationDto": {
        "type": "object",
        "description": "Operation raw parameters (XDR-decoded full details).",
        "required": ["op_type", "application_order", "details"],
        "properties": {
          "application_order": {
            "type": "integer",
            "format": "int32",
            "description": "Application order within the transaction (1-based, matches Horizon\n`paging_token` convention)."
          },
          "details": {
            "description": "Full operation details (type-specific JSON)."
          },
          "op_type": {
            "type": "string",
            "description": "Operation type tag (e.g. `\"payment\"`, `\"invoke_host_function\"`)."
          },
          "result_code": {
            "type": ["string", "null"],
            "description": "Per-operation result code from the transaction result XDR, using the\nXDR library's variant names: `\"Success\"`, `\"LowReserve\"`, `\"Trapped\"`,\nop-level rejections as `\"OpNoAccount\"` etc. Present on failed\ntransactions too — the failing op's code is the fail reason (task\n0352). `None` when the result XDR carried no per-op array\n(validation-level failures) or was unavailable."
          }
        }
      }
    },
    "securitySchemes": {
      "api_key": {
        "type": "apiKey",
        "in": "header",
        "name": "x-api-key"
      },
      "bearer_jwt": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  },
  "security": [
    {
      "api_key": []
    },
    {
      "bearer_jwt": []
    }
  ]
}
