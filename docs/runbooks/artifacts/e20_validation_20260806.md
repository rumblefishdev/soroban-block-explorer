# E20 Re-validation — 20260806

Generated by `compare_e20_chq.py` (workstation variant: read-only
`chq` + the `operation_pools` seek — the CURRENT production read path from task 0365) on 2026-08-06. The script was a one-shot tool and is **not in the
repo** — it was retired unmerged in task 0455 because it used Horizon as the
oracle, which the 2026-08-17 rule retires in favour of raw XDR; recover it
from commit `cfd9bb0f` if a rerun is ever wanted, and re-point the oracle
first. Run as the last outstanding item of task **0403**
(post-ship validation of the 0365 lptxs swap); supersedes nothing — the
2026-06-18 verdict stands, this confirms it through the new driver.

## Method

Identical to the 2026-06-18 run (see `e20_validation_20260618.md`) with two
mechanical substitutions: ClickHouse queried via the read-only `chq` wrapper
from a workstation (no box SSH), and pool→tx attribution read from
`operation_pools` (pool_id-leading PK seek) instead of the
`has(pool_ids, …)` scan — the same-day SQL diff in task 0403's history shows
the two drivers byte-identical, and this run exercises the one production
actually uses.

**Posture note:** Horizon is a cross-check reference for recall/precision, not
an arbiter — it has misled this project before (tasks 0256/0430; raw XDR is
the arbiter). A discrepancy is a lead to investigate, never an automatic
"we are wrong".

## Result — PASS

```
evaluated=50  skipped(retention)=0
mean_recall=1.0000  mean_precision=0.9991
min_recall=1.0000   min_precision=0.9755
```

Recall is exactly 1.0 on every pool: ClickHouse misses **nothing** Horizon
attributes.

One pool flagged (`ad45b80a…`, precision 0.975): five transactions attributed
by CH and absent from Horizon's listing. All five investigated:

- every one is a **failed transaction** (`successful = false`, confirmed both
  in CH and on Horizon's `/transactions/{hash}`),
- the sampled one is a `LiquidityPoolDeposit` (type 22) naming the pool in its
  operation body — attribution correct by construction.

Horizon's `/liquidity_pools/{id}/transactions` excludes failed transactions by
default; this project deliberately indexes them. The entire precision gap is
that endpoint-semantics difference, not over-attribution.
